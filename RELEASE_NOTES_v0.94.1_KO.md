# v0.94.1 릴리스 노트

v0.94.1은 Steam 일본어판 PK 1.1.7, Steam build 18823764용 v0.94.0
핫픽스입니다. v0.94.0의 번역·이미지·글꼴 선택·가상 히메 이름 용량 보정을
모두 유지하면서, 배포 패처의 더블클릭 실행과 동적으로 생성된 가문명 표기를
수정합니다.

2026-08-20 승인된 빌드를 실제 Steam 설치본에 적용하고 게임 기동과 파일 단위
검증을 완료한 정식 릴리스입니다.

## 배포 파일

- 파일: `NOBU16_PK_Korean_Patch_Steam_1.1.7_v0.94.1.zip`
- 크기: 929,827,570바이트
- SHA-256: `2616FDC84EC2E5E88D08FA6E434385E5955B6B110383B5D9F304A383DC093C6F`
- 패처: `N16KoreanPatcher.exe` 0.2.9,
  SHA-256 `D50DE426D5330B31E6658176A5F56388CEED6D6CD1392A150219F810887C904C`

최종 ZIP에는 최상위 파일 13개만 들어 있습니다. 새 폴더에 다시 풀어
`SHA256SUMS.txt`가 추적하는 12개 구성 파일의 SHA-256을 전부 대조했고,
7개 서명 번들의 공개 메타데이터와 릴리스 버전 0.94.1을 확인했습니다.

## 배포 패처 더블클릭 실행

- 공개된 v0.94.0 ZIP의 `N16KoreanPatcher.exe`는 명령줄 하위 명령은
  실행되지만, 인자 없이 더블클릭하면 `이 개발 빌드는 명령줄 하위 명령만
  지원합니다`라는 오류로 종료되는 것을 확인했습니다.
- GitHub v0.94.0 ZIP에서 직접 꺼낸 EXE와 당시 Steam 설치본 EXE의 SHA-256은
  모두 `BD3C634E428C1AE18304911C030A45FB23B1C722E0BB99EEF42D7CD7085179A7`로
  같았습니다. 사용자 환경의 개별 손상이 아니라 공개 자산 자체의 빌드 구성
  문제입니다.
- 원인은 일반 release 빌드에서 통합 메뉴 기능 플래그가 기본값이 아니었던
  것입니다. v0.94.1부터 인자 없는 기본 release 빌드는 통합 메뉴를 포함하며,
  명령줄 전용 개발 빌드는 `--no-default-features`로 명시해야만 만들 수 있습니다.
- ZIP 생성기는 포장할 정확한 EXE를 빈 임시 폴더에서 인자 없이 실행합니다.
  개발 빌드 전용 오류가 나오면 포장을 거부하고, 제품 빌드가 정상적인
  `NOBU16PK.exe` 설치폴더 검사까지 도달해야만 계속 진행합니다.
- 2026-08-20 실제 Steam 게임 폴더의 파일 탐색기에서 v0.94.1 패처를
  더블클릭했습니다. `0.2.9` 프로세스가 Steam build 18823764 설치 경로를
  승인하고 메뉴 입력 대기 상태로 유지되는 것을 확인했습니다.

패처 제품 버전은 0.2.9입니다.

## 동적 신세력 가문명

- 순정 일본어 PK `MSG_PK/JP/msgui.bin` ID 1139는 `%s家`입니다. 기존 한국어
  `%s 세력`의 `세력`은 원문에 있는 표현이 아니며, 이 서식을 사용하는 독립
  신세력에서 일반 가문과 다른 접미사가 표시되는 원인이었습니다.
- ID 1139 하나만 `%s 가문`으로 바꿨습니다.
- 성씨 원본의 후행 공백은 장수의 성·이름 조합에 필요하므로 수정하지
  않았습니다. 대신 실행 파일의 공통 UTF-16 서식 함수가 정확히 `%s 가문`
  서식을 처리할 때만 첫 번째 인자 끝의 ASCII 공백 U+0020을 제거합니다.
- 문자열 내부 공백, 탭, 전각 공백과 다른 `%s` 서식은 건드리지 않습니다.
  공백이 없는 등록 무장·사용자 입력 이름도 같은 경로를 안전하게 통과합니다.

표시 계약은 다음과 같습니다.

- `마츠다이라 ` + `%s 가문` → `마츠다이라 가문`
- `사용자입력명` + `%s 가문` → `사용자입력명 가문`

정적 패치는 PC 가로형, PC 가로형 XInput, PC 세로형, PC 세로형 XInput과
Steam Deck 실행 파일에 적용됩니다. 알려진 v0.94.0 최종 해시와 후크 위치의
선행 바이트가 모두 맞을 때만 적용되며, 재적용은 같은 결과를 유지하고 다른
바이트는 거부합니다.

## 검증

- `MSG_PK/JP/msgui.bin` 5,100개 문자열 중 ID 1139 하나만 바뀌는 것을
  압축 전후 해시와 전체 문자열 비교로 확인했습니다.
- 새 `msgui.bin`의 크기는 122,733바이트, SHA-256은
  `61D1C6691B97058D02753C088F95FB044DA09E015520A9FA74CC2050C7113EC9`입니다.
- 실행 가능한 래퍼 하네스에서 후행 공백 1개, 여러 개, 공백 없는 사용자 입력과
  출력 용량 경계를 각각 검사했습니다.
- PC 여섯 정적 fixture와 Steam Deck fixture의 정확한 최종 크기·해시,
  선행 바이트 거부와 멱등성을 검사했습니다.
- 기존 공식 서명 PC 가로형 XInput 및 Steam Deck 번들을 순정 실행 파일에
  적용한 뒤 새 후크 점프·래퍼 바이트·최종 해시를 확인하고 순정 복구까지
  통과했습니다.
- A `SeoulHangang ExtraBold`와 B `그리운 경찰감성체` 리소스 번들은 각각
  147개 파일의 적용·재검증·복구를 통과했습니다. 두 번들 모두 새 ID 1139를
  포함하며 기존 글꼴 선택 차이는 유지합니다.
- 공개 v0.94.0의 잘못된 EXE는 새 ZIP 포장 검사에서 거부되고, v0.94.1 제품
  EXE는 같은 검사에 통과하는 것을 확인했습니다.
- 최종 ZIP의 정확한 EXE를 인자 없이 실행하는 제품 빌드 검사, 새 폴더 압축
  해제, 13개 항목 목록과 12개 내부 SHA-256 대조를 통과했습니다.

## Steam 설치본 적용 검증

- 2026-08-20 게임과 패처 프로세스가 모두 종료된 것을 확인한 뒤 Steam JP
  PK 1.1.7, build 18823764 설치본에 적용했습니다.
- 기존 v0.94.0 관리 설치를 검증된 백업으로 순정 복구한 뒤, v0.94.1 B
  `그리운 경찰감성체` 리소스와 PC 가로형 XInput, Steam Deck 정적 패치를
  차례로 적용했습니다.
- 세 번들 모두 패처의 재검증 결과 `already_applied: true`, 보고 버전
  `0.94.1`을 확인했습니다.
- 실제 설치 파일의 최종 SHA-256은 다음과 같습니다.

  - `NOBU16PK.exe`: `A69498D250130484FF4B632CC51E6D47F604F2CFB40F30EB3084628140BAC3A0`
  - `NOBU16PK_XINPUT.exe`: `EB43E5E8A797445F17E12AC12F910827EF200D3054A61AE84B33F6AC8D8E8F8D`
  - `NOBU16PK_SD.exe`: `FDB003ED95C0462275BF97872FA18254CBFC2C855EA21C1F3DBC8EEEE44895C3`
  - `MSG_PK/JP/msgui.bin`: `61D1C6691B97058D02753C088F95FB044DA09E015520A9FA74CC2050C7113EC9`
  - `N16KoreanPatcher.exe`: `D50DE426D5330B31E6658176A5F56388CEED6D6CD1392A150219F810887C904C`

- 게임이 완전히 종료된 상태에서 Steam 런처로 `NOBU16PK.exe`를 새로
  실행했습니다. 해상도는 변경하지 않았으며 게임 로그 기준 화면 크기는
  2560×1600, 실제 뷰는 2560×1440입니다. 따라서 전체 프로세스 재실행 조건을
  충족했습니다.
- 오프닝, 타이틀 화면, 한국어 메인 메뉴와 대기 자동 연출 후 타이틀 복귀까지
  확인했습니다. 최소 214초 동안 프로세스가 응답 상태를 유지했고 Windows
  Application Error·WER의 `NOBU16PK.exe` 크래시 이벤트는 0건이었습니다.
- 저장 데이터를 불러오지 않았으므로 독립 신세력의 실제 상단 UI·가문명 표기는
  아직 확인하지 않았습니다. 게임은 사용자가 이어서 검사할 수 있도록 메인
  메뉴에 실행 상태로 두었습니다.

## 후속 런타임 확인

- 독립 신세력 저장 데이터를 불러온 뒤 실제 상단 UI·가문명 표시를 확인합니다.
