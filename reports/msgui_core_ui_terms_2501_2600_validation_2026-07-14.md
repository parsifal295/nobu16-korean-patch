# msgui ID 2501-2600 번역 배치 검증 기록

## 산출물과 배포 경계

- 배치: `KR_PATCH_WORK/data/translations/msgui_core_ui_terms_2501_2600.v0.1.json`
- 배치 SHA-256: `64CB32EB5CB13DEBF3B16CDA3446571481241D11A91BB82339A12AE85890B18F`
- 성격: 개발 전용. 공식 다국어 원문 일부를 포함하므로 공개 패치에는 이 파일을 직접 넣지 않는다.
- 상태: 초벌 `translated`. 실제 화면에서 문맥과 폭을 확인하기 전에는 `reviewed`로 올리지 않는다.
- 작업 범위 밖인 설치 게임 파일, 공개 overlay, Font-v4, 성 이름 workstream, 실행 파일과 레지스트리는 수정하지 않았다.

## 범위와 원문 해시 게이트

- 조사 범위: ID 2501-2600, 총 100개
- 배치 수록: 100개
- 연속 범위: 최솟값 2501, 최댓값 2600, 누락·중복 ID 0개
- 빈 한국어 번역: 0개
- `source_en`과 `source_sc_utf16le_sha256`: P3 기준 100개 모두 일치
- 원본 메타 SHA-256: `997586BC67C788821352192116C195EE753E58FE91FC825669C7E151374C8B9E`
- 원본 P3 카탈로그 SHA-256: `80E1B46F829F810FA4E55C412D92ED8FC8B48CEFB11FC826C3CD81FED83232BA`

## 주요 번역 판단

- ID 2501·2556·2582는 기존 ID 2392 `보급 군량`을 이어 각각 `보급 군량 수입`, `최대 보급 군량`, `보급 군량 보충`으로 통일했다.
- ID 2519-2521의 `優/良/凡`은 평가 등급 문맥에 맞춰 `우수/양호/보통`으로 옮겼다.
- ID 2523, 2555, 2562의 `攻城`은 용어집의 `공성전`을 적용했다.
- ID 2530·2537·2538의 `感状`은 전공을 칭찬해 내리는 문서라는 역사 용어를 살려 `감장`으로 통일했다. `論功行賞=논공행상`, `勲功=공훈`과 구분한다.
- ID 2548 `功績`은 기존 용어 판단과 맞춰 `공적`으로 옮겨 `勲功=공훈`과 구분했다.
- ID 2559 `外様家宰`는 인접 배치 ID 2405·2406·2421·2423의 결정과 맞춘 `외양 가재`를 사용했다.
- ID 2576 `家紋`은 기존 ID 1629 `가문 문양`을 이어 `가문 문양 선택`, ID 2579 `二つ名`은 인접 배치 ID 2462와 맞춰 `이명 선택`으로 옮겼다.
- ID 2584 `引抜`는 기존 ID 1999 `포섭`을 이어 `%s을(를) %s와 함께 포섭`으로 옮기고 두 `%s`의 의미 순서를 유지했다.
- ID 2585 `腰兵糧`은 기존 ID 1403과 맞춰 `휴대 군량`, ID 2597 `郡代/城代`는 기존 ID 1351/1349와 맞춰 `군대/성대`를 사용했다.
- ID 2598은 기존 `다이묘 군단`, `통치 범위`를 이어 `다이묘 군단의 통치 범위 내 성`으로 옮겼다.
- ID 2600 `他勢力`은 다음 ID 2601 `自勢力`과 짝을 이루도록 `타세력`으로 옮겼다.

## 동적 토큰·구조 문자 계약

- printf 포함 항목 10개: ID 2524, 2530-2532, 2546, 2584, 2589, 2592, 2595, 2599
- 모든 항목에서 printf 토큰의 종류와 순서를 SC 원문과 동일하게 유지했다. ID 2589는 EN 표기의 인수 순서가 잘못되어 있으므로 JP·SC의 `%s`, `%d` 순서를 따랐다.
- ESC, PUA, 줄바꿈, 기타 제어문자, 미확인 percent 항목: 모두 0개
- ID 2532의 전각 물결표 `～`, ID 2568·2569·2600의 전각 괄호, ID 2595·2599의 전각 콜론을 유지했다.
- ID 2525-2528의 그리스 문자 `Κ/Λ/Μ/Ν`는 모든 언어에서 같은 구조용 표식이므로 변경하지 않았다.
- ID 2558과 2570-2572는 SC/TC/EN이 의도적으로 반각 공백 하나인 자리다. JP 전용 표기나 읽기를 새로 노출하지 않고 `U+0020` 하나를 그대로 유지했다.

## SC 원문과 바이트 동일한 항목

번역 상태와 ID 연속성을 유지하되 실제 메시지 바이트 교체가 필요 없는 항목은 12개다.

- 구조용 표식: ID 2525-2528
- 서식 문자열: ID 2531 `×%d`, ID 2595 `%s：%s`, ID 2599 `：%s`
- 의도적 반각 공백: ID 2558, 2570-2572
- 자리표시자: ID 2588 `○○`

따라서 배치 병합은 카탈로그 행 100개를 갱신하지만, 이 범위에서 실제 바이너리 문자열이 달라지는 항목은 88개다.

## 독립 병합·검증·결정성 빌드

P3 카탈로그 복사본에 이번 배치 하나만 병합해 검사했다. 검증 산출물은 Git에서 제외되는 `KR_PATCH_WORK/tmp/translate_2501_2600_validation` 아래에만 두었다.

- merge-batch: 입력 100개, 갱신 100개
- 병합 카탈로그 SHA-256: `9193E49167601295BFB3BFC3496ACB3252A4876FF2D5BD0A79462587EF13C652`
- 병합 보고서 SHA-256: `A82600AA670A34B4AD66C59A00F104A20FF9EED776E16D87C67A6D59B9C6634C`
- validate: 5,100행, `valid=true`, 오류 0, 경고 0
- 상태: `translated=379`, `untranslated=3683`, `empty=1038`
- buildable: 379개
- 검증 JSON SHA-256: `6B1CCF29DC79C5DBF2BE942D22B8746AB252632BC60E3FDA19B8584293B2BA65`
- 실제 바이너리 변경: 367개(P3 기존 279개 + 이번 범위 88개)
- 대상 크기: 86,403바이트
- 대상 SHA-256: `42F72FDCA0A99B2DC83CCAA13FD0F5D557C308C8E6D9CA8F9952B2009F300F89`
- 대상 raw 크기: 86,040바이트
- 대상 raw SHA-256: `CE7DAD664FF741F3B852D2490A4F86F82A9A6DC921DC95AC36415A8F61F9281C`
- 필요 글리프: 290자
- 한글 음절: 257자
- 빌드 manifest SHA-256: `BBED361A4E06B000D806960F05E521200F03892F777264F58D1DD04E10E9800C`
- glyph demand SHA-256: `2E794360A872828F534BB9E6EAE9E3A2379BE38D63D18C3F735EFFD99ECFCC91`
- 동일 입력 반복 빌드: `msgui.bin`, manifest, glyph demand가 모두 바이트 단위로 일치
- `installed_game_files_modified=false`

## 설치본 무변경 확인

검증 뒤 설치본은 다음 고정 해시를 유지했다.

- `MSG_PK/SC/msgui.bin`: `C2C69FDF09D9BE06E14F03C4F40562ADD0CA247EE0D50FC3E06EF501524B5E82`
- `MSG_PK/SC/msgdata.bin`: `0586C269D381AA1FD75C39802AC01C697B5E8469A9C1ABE693BA5EDB2E658B3E`
- `RES_SC/res_lang.bin`: `916759185E9D64E487530DCA760CD36AE1FCFF021F39CEB1658837FE60AE0D99`

이번 검증은 파일 전용 오프라인 빌드만 수행했으며 게임 실행, 프로세스 메모리 접근, DLL 주입·후킹, 실행 파일·레지스트리 변경을 하지 않았다.
