# msgui ID 3901-4000 번역 배치 검증 기록

## 결과

- 대상 범위: ID 3901-4000, 정확히 100행 조사
- 번역 배치 엔트리: 100개
- canonical `empty`: 0개
- 구조 공백 byte-equal 유지: 4개(ID 3942, 3988-3990)
- 교차 검수 반영: 2개(ID 3986, 3997)
- 병합 및 stock 대조 검증: `valid=true`, 오류 0개, 경고 0개
- invariant override: 0개
- 설치 게임 파일 변경: 없음

배치 파일은 공식 다국어 원문을 포함하는 개발 전용 자료다. 공개 패치에는 직접 넣지 않고 추후 source-free overlay만 생성한다.

## 산출물

- 배치: `KR_PATCH_WORK/data/translations/msgui_core_ui_terms_3901_4000.v0.1.json`
- 배치 SHA-256: `9AE2EEA6320D28E8D1CC7945F38DFE4928CDDF4DA7E4F31EE1D72F96CD20BE08`
- 기준 meta SHA-256: `997586BC67C788821352192116C195EE753E58FE91FC825669C7E151374C8B9E`
- 기준 P3 catalog SHA-256: `80E1B46F829F810FA4E55C412D92ED8FC8B48CEFB11FC826C3CD81FED83232BA`
- 임시 단독 병합 catalog SHA-256: `65C37408C2DC10040B568B468D31F60A96D1EC3A772E10B959E838024CB30685`
- batch merge report SHA-256: `5C1993A218A5E1FC83C7828AE03A56E8D144417628A43B80C21FF1430EEB6CCA`
- 단독 validation JSON SHA-256: `4EA5D4B261B86B4F5F8B5995072764EC0E80193A0DB761E17AAD217585EEAC69`

## 범위와 구조 슬롯 처리

ID 3901-4000의 EN·JP·SC·TC 원문을 모두 대조했다. 네 언어 전체가 빈 canonical `empty` 행은 없다.

ID 3942와 3988-3990은 EN·SC가 반각 공백 `U+0020` 한 글자이고 TC도 공백만 사용하는 비활성 구조 슬롯이다. JP에만 명소 조건이나 직담 요구 문구가 남아 있으나 SC 실행 경로에서 임의로 되살리지 않고 한국어도 반각 공백 한 글자를 그대로 유지했다. 따라서 100개 엔트리 중 catalog 상태·검토 정보는 100개 모두 갱신되고 실제 SC 문자열 바이트가 달라지는 항목은 96개다.

## 교차 검수 반영 내역

- ID 3986: `자세력에 신종하도록 종속 대상을 바꿉니다` → `종속 대상을 자세력으로 변경합니다`
- ID 3997: `자세력에 신종하도록 요구합니다` → `자세력에 종속할 것을 요구합니다`

두 문구는 `臣従` 관계의 주체와 방향을 더 분명히 하도록 다듬었다. printf·ESC·PUA·제어문자·줄바꿈 불변식에는 변화가 없다.

## 주요 번역 판단

- ID 3901의 EN `personality`와 SC `性格`은 실제 문맥과 어긋난다. JP `声色` 및 기존 등록 무장 편집 UI를 따라 `선택한 음성으로 변경합니다`로 옮겼다.
- ID 3902-3909는 `군단장`, `군단 전략`, `가재`, `가재 특성`, `지원 거점`, `원군`을 누적 용어로 유지했다. ID 3904는 SC 기준 printf 순서 `%s`, `%s`, `%d`를 보존했다.
- ID 3910/3911은 역사 이벤트 분기를 `사실 전개`/`가상 전개`로 짝지었다.
- ID 3913-3918은 `城役割=성 역할`, `城主=성주`로 통일하고 성주 부재에 따른 역할 해제와 새 성주 임명을 구분했다.
- ID 3919/3987은 `約定=약정`, `知行=지행`, `安堵=소령 안도`를 기존 지행·안도 계열과 맞췄다.
- ID 3922-3930은 `방위 거점`, `보급 거점`, `방위 구획`, `방위 병력`, `휴대 군량`을 기존 명칭으로 유지했다.
- ID 3931-3933은 공성 준비 상태를 `공성전 불가/가능`, `본성`, `방위 구획`, `방위 무장`으로 구성했다. 상태 아이콘용 `Σ`, ESC 색상 코드, 전각 공백, 줄바꿈을 원문 구조대로 보존했다.
- ID 3934-3941은 조건형 조각으로 `전선`, `후방`, `상업`, `석고`, `온천향`, `남만사`, `사찰`, `명소`를 적용했다.
- ID 3943은 EN 문자열이 손상되어 있으므로 JP·SC·TC와 동일 SC 원문의 기존 ID 1738을 따라 `%s의 %s`로 옮겼다. ID 3944의 `二つ名`은 기존 용어 `이명`을 사용했다.
- ID 3946-3987은 동맹 파기·외교 금지·영지 반환·금전/가보/영지 요구·신종을 다루는 직담 교섭 문구다. `交渉値=교섭치`, `臣従`은 제도·행위명에서 `신종`, 관계 설명에서 `종속`, `婚姻同盟=혼인 동맹`, `本拠=본거지`로 구분했다.
- ID 3980-3983은 `군 제압 속도`, `적 부대 피해`, `전법·요충지·설비 게이지`, `소속 성 군량 수입` 효과로 정리했다.
- ID 3991은 `奉行=봉행`, ID 3992는 `合戦=전투`, ID 3994는 정책 효과의 부호 포함 `%+d`를 유지했다.
- ID 3999는 세력 총 상업과 가보 등급의 관계를 설명하면서 세력 목표 이름을 감싸는 ESC 색상 코드를 보존했다.
- ID 4000은 상대가 이미 보유한 등급 이하의 가보를 교섭 재료로 쓸 수 없다는 의미로 옮겼다.

동일 SC 원문 해시의 기존 개발 배치도 대조했다. ID 3943/1738은 모두 `%s의 %s`다. 구조 공백 4개도 기존 반각 공백 슬롯 107개와 모두 byte-equal이며, 이번 배치가 새로 만든 번역 불일치는 0개다.

## 형식 invariant

printf 토큰이 있는 항목은 37개다.

- `%s`: ID 3902, 3903, 3909, 3913, 3934-3941, 3954, 3960, 3979, 3992, 3996, 3999, 4000
- `%s`, `%s`: ID 3914, 3917, 3943, 3998
- `%s`, `%s`, `%d`: ID 3904, 3919, 3987
- `%s`, `%d`: ID 3915
- `%s`, `%d`, `%s`: ID 3918
- `%d`: ID 3931, 3969, 3972, 3975, 3984, 3991, 3995
- `%d`, `%d`, `%d`: ID 3932
- `%s`, `%+d`: ID 3994

모든 항목에서 printf 토큰의 종류와 순서가 SC 기준과 일치한다. EN 토큰 순서가 손상되거나 다른 ID 3904, 3915, 3994도 실제 SC 실행 문자열을 기준으로 구성했다.

SC 원문과 한국어의 줄바꿈 수는 다음과 같이 일치한다.

- 줄바꿈 1개: ID 3913, 3917, 3918, 3931-3933, 3999
- 줄바꿈 2개: ID 3914, 3915, 3919, 3987

ESC 코드는 ID 3931의 `ESC CP`/`ESC CZ`, ID 3932/3933의 `ESC CS`/`ESC CZ`, ID 3999의 `ESC C2`/`ESC CZ`를 순서와 위치 그대로 보존했다. ID 3931/3932는 상태 뒤와 다음 줄 앞의 전각 공백 `U+3000` 두 개, ID 3933은 다음 줄 앞의 전각 공백 한 개를 유지했다. 이 범위에는 PUA나 기타 제어문자가 없고 invariant override도 없다.

## 단독 병합·검증

P3 canonical 복사본에 이번 배치만 병합했다.

- `batch_entries=100`
- catalog metadata 변경: 100개
- 전체 행: 5,100개
- 상태: `translated=379`, `untranslated=3683`, `empty=1038`
- buildable: 379개
- 검증: `valid=true`
- 오류: 0개
- 경고: 0개

stock 4개 언어 `MSG_PK/*/msgui.bin`의 크기·packed SHA-256·raw SHA-256도 meta와 모두 일치했다.

## 단독 빌드와 결정성

같은 단독 병합 catalog를 서로 다른 두 출력 디렉터리에 연속 빌드했다.

- 대상 `msgui.bin` 크기: 88,025바이트
- 대상 `msgui.bin` SHA-256: `58DFC770E93CA2695CEE06C5A0C665E486F22C72752E9530EFBAF3FAEC63FD39`
- 대상 raw 크기: 87,656바이트
- 대상 raw SHA-256: `4A43B26CC6E9232782F2360F04C8D92E0102BD81274EEF4992BA345F0CD3DB63`
- 전체 실제 바이너리 변경: 375개(P3 seed 포함)
- 글리프 수요: 318자
- 한글 음절 수요: 291자
- build manifest SHA-256: `07DCB8279DB80AB117BB5AF579A995E1D046023B599889E4C0ACBDB900864EBE`
- glyph demand SHA-256: `D5382C787295D39C428E7FBFF0783842D38ACC4EBE1ABAE39C9C5597332BB80F`

두 빌드의 `msgui.bin`, manifest, glyph demand가 모두 바이트 단위로 일치했다.

검증 뒤 설치본 `MSG_PK/SC/msgui.bin`은 stock SHA-256 `C2C69FDF09D9BE06E14F03C4F40562ADD0CA247EE0D50FC3E06EF501524B5E82`, `RES_SC/res_lang.bin`은 `916759185E9D64E487530DCA760CD36AE1FCFF021F39CEB1658837FE60AE0D99` 그대로다. 결정성 산출물은 무시된 `KR_PATCH_WORK/tmp/revalidate_3901_4100/3901_4000/build_a` 및 `build_b` 아래에만 생성했다.
