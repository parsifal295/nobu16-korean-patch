# v0.90.3 릴리스 노트

v0.90.3은 v0.90.2를 기준으로 XINPUT 컨트롤러 실행 파일을 안정화하고,
건의 안내 대사 한 건의 잘못된 런타임 어미 조합을 교정한 릴리스입니다.
일반 `NOBU16PK.exe`의 내용은 v0.90.2와 동일합니다.

## 변경 사항

- `NOBU16PK_XINPUT.exe`에서 방향 입력이 동작하지 않던 문제 수정
- A/B/X/Y 버튼 배열이 뒤바뀌던 문제 수정
- 패드 방향 입력에 마우스 커서가 따라 움직이던 문제 수정
- XINPUT판의 마우스 커서를 숨기되 컨트롤러 입력 경로는 유지
- 일반 `NOBU16PK.exe`의 마우스 입력 경로는 변경하지 않음
- 건의 안내 대사가 `필요 시 참조해저것`으로 표시되던 문제를
  `필요할 때 참조해 보시오.`로 수정
- v0.90.3 배포 패처에 한해 Steam AppManifest·관리 설치 경로 검사를
  일시적으로 생략
- 순정 Steam JP PK v1.1.7 파일 SHA-256, 패치 번들 서명, 결과 해시 검증은 유지

## 컨트롤러 실행 방법

1. 패처에서 `컨트롤러 지원 → 실험적 지원 사용`을 선택합니다.
2. 생성된 `NOBU16PK_XINPUT.exe`를 Steam 라이브러리에 비 Steam 게임으로
   등록합니다.
3. 등록한 항목의 컨트롤러 설정에서 Steam Input을 켭니다.
4. 컨트롤러를 먼저 연결한 뒤 Steam에서 `NOBU16PK_XINPUT.exe`를 실행합니다.

XINPUT판에서는 마우스를 사용할 수 없습니다. 마우스로 플레이할 때는 일반
`NOBU16PK.exe`를 실행하십시오.

## 설치 전 주의

- Steam JP PK v1.1.7, Steam build `18823764` 전용입니다.
- 기존 패치가 적용되어 있다면 먼저 해당 패처로 원본 복구하거나 Steam 파일
  무결성 확인을 완료한 뒤 v0.90.3을 적용하십시오.
- 세로형·가로형 또는 컨트롤러 지원 여부를 바꿀 때도 먼저 패처 메뉴 `2`로
  원본 복구한 뒤 다시 적용하십시오.
