# v0.91.1 릴리스 노트

v0.91.1은 XInput 스틱 중립값 처리, 성곽 조영 문구, 가문명 표기·독음과
세력 정보 화면의 긴 가문명 표시를 보정한 안정화 릴리스입니다. v0.91.0의
인물 이름 레이아웃, 이벤트 명판 자동 폭, 컨트롤러 재연결·숫자 입력·LB/RB·
Select 보정과 144개 리소스는 모두 포함합니다.

## 주요 변경 사항

### XInput 스틱 데드존 — #104

- XInput 브리지가 원본 게임의 스틱 데드존 처리를 건너뛰어 중립 축의 작은
  노이즈도 이동 입력으로 전달하던 문제를 수정했습니다.
- 좌·우 스틱에 원본과 같은 `dx²+dy² < 4096` 원형 데드존을 적용했습니다.
- 패드를 건드리지 않아도 커서가 한쪽으로 움직이거나, LB 카메라 줌이 한
  방향으로 되돌아가던 증상을 방지합니다.
- 가로형·세로형 XInput 실행 파일과 기존 v0.91.0 관리 설치본 승격 경로를
  함께 검증했습니다.

### 성곽 조영 문구 — #105

- 3레벨 효과의 `야마시로의 낙석를 해방`을 `산성의 낙석을 해방`으로
  수정했습니다.
- 같은 효과군의 동적·정적 문구 네 항목을 함께 보정하고 나머지 문자열과
  바이너리 메타데이터가 유지되는지 검증했습니다.

### 가문명과 독음 — #106

- `宗家`가 일본식 독음 `소케`로 표시되던 슬롯을 `소 가문`으로 수정했습니다.
- 가문명 표시 블록 258개가 모두 `○○ 가문` 형식인지 전수 검사했습니다.
- 가문명 독음 슬롯 258개도 전수 조사했습니다.
  - 일반 무장명과 한자가 같은 190개는 검증된 한국 한자음을 재사용했습니다.
  - 복합 가문명 68개는 구성 한자의 한국 한자음을 별도 검토했습니다.
  - 일본식 독음으로 남아 있던 256개를 교정하고, 이미 올바른 `모리`, `아소`
    두 항목은 유지했습니다.
  - 주요 교정: `大友: 오토모→대우`, `龍造寺: 류조지→용조사`,
    `宗: 그렇군→종`.
- 원문 `家`는 `가문`, `勢力`은 `세력`으로 유지해 문맥상 구분을 보존했습니다.

### 세력 정보의 긴 가문명

- 세력 정보 화면은 전체 열 폭에 여유가 없어 외곽 가문명 열 190px를
  그대로 유지했습니다.
- 해당 화면의 정확한 행 컨테이너만 식별해 내부 텍스트 폭을 `170→146px`로
  제한했습니다.
- 문자열을 자르지 않고 게임의 기존 가로 축소 렌더링을 사용하므로 긴
  가문명이 다이묘 열을 침범하지 않습니다.
- 가로형·세로형과 각 XInput 실행 파일에 같은 조건을 반영했습니다.

## 패처 및 호환성

- 패처를 0.2.3으로 갱신했습니다.
- v0.91.1 공통 리소스 번들은 필수·선택 파일 144개의 적용·검증·복원을
  지원합니다.
- 정적 패처는 순정 원본 6개 경로와 기존 v0.91.0/v0.91.1 실행 파일 승격
  경로를 검증합니다.
- 승격 과정에서 파일이 이미 최종 상태가 된 경우에도 오류로 종료하지 않고
  정상 적용 결과를 반환하도록 보정했습니다.

### v0.91.0에서 갱신

1. 게임과 런처를 완전히 종료합니다.
2. v0.91.1 ZIP 전체를 게임 최상위 폴더에 덮어씁니다.
3. 새 패처의 메뉴 `2`로 기존 패치를 원본 복구합니다.
4. 패처를 다시 실행해 메뉴 `1`에서 원하는 설정으로 v0.91.1을 적용합니다.

수동 수정 때문에 복구 검증이 실패하면 Steam 파일 무결성 확인으로 순정 JP
1.1.7 상태를 준비한 뒤 적용하십시오.

## 검증 결과

- `cargo test --workspace --features v0910-release`: 24개 테스트 통과
- `cargo clippy --workspace --all-targets --features v0910-release -- -D warnings`: 통과
- 이슈 #105·#106 문자열·독음 회귀 테스트 3개 통과
- 정적 실행 파일 6종 생성 SHA 계약 통과
- 가로형 XInput 순정 설치 및 v0.91.0→v0.91.1 승격 E2E 통과
- 세로형 XInput v0.91.0→v0.91.1 승격 E2E 통과
- 144개 공통 리소스 순정 설치·검증 E2E 통과
- Steam 가로형 XInput 최종 설치본에서 패처 검증 통과 후 새 프로세스 시작 유지
- Steam 세로형 일반 실행 파일을 1920×1080에서 완전 종료·재실행해 실제 게임
  화면이 렌더링되고 15초 이상 유지되는 것을 확인했습니다.
- Steam 비게임 바로가기에서 세로형 XInput 실행 파일을 완전 재실행해 오프닝
  영상과 1920×1080 타이틀 화면이 렌더링되고 15초 이상 유지되는 것을 확인했습니다.
- 세력 정보 내부 폭 수정은 동일한 런타임 바이트를 임시 적용해 행을 재생성한
  실제 화면에서 사용자가 다음 열 침범 해소를 확인했습니다.

## 배포 파일

- 파일명: `NOBU16_PK_Korean_Patch_Steam_1.1.7_v0.91.1.zip`
- 크기: `522,940,855` bytes
- SHA-256:
  `46BC24EC6EE8F6C923F27BC86E35B5BC2D2D1E05D78C7D37C6AD3307C673EA99`
- 패처 `N16KoreanPatcher.exe` SHA-256:
  `CFE5FA6BDF8EC7A2638B3CFC469B1476178B7F100774B7C853A3E296AC3ED227`
- 공통 리소스 번들 SHA-256:
  `437CF8C908CEFB7C70400E680BCC70FB314C6A8ACE5EC8D48FD50A3DC8213C68`
- 가로형 일반 EXE 결과 SHA-256:
  `42C1238E0337D81E996322CFF73E04397CDC22D4ED33F619D3344C63192DE852`
- 가로형 XInput EXE 결과 SHA-256:
  `EAC0FAFA69951CB4FDDE2683828B49777F95B374A5F9608EA2D16CA387985561`
- 세로형 일반 EXE 결과 SHA-256:
  `01029C76AAF5C46D9A1B700D9B5985BF77213C7C26B30DB20F1AF38488C5D29F`
- 세로형 XInput EXE 결과 SHA-256:
  `A28744A9F79B8DB99A04B0B380B11B3A00A913F709F47126C34DD4D33713D935`

ZIP 내부 `SHA256SUMS.txt`에서 모든 구성 파일 SHA-256을 확인할 수 있습니다.
