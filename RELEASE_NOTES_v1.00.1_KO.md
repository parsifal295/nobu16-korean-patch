# 신생 PK 한국어 패치 v1.00.1 핫픽스

v1.00 설치 중 `SCENARIO_PK/scedaPK13.n16`에서 ‘원본 파일 검사 실패’가 나오며 적용이 중단되는 문제를 수정했습니다. 정상 게임 파일을 패처가 잘못 판단하던 오류로, 글꼴 A/B 모두 수정했습니다. 이용에 불편을 드려 죄송합니다.

v1.00의 번역과 수정 사항을 모두 포함한 누적 패치입니다. 이번 핫픽스의 게임 내 번역·이미지·기능은 v1.00과 같습니다.

## 다운로드

[v1.00.1 다운로드](https://github.com/parsifal295/nobu16-korean-patch/releases/tag/v1.00.1)

첨부 파일 중 **`NOBU16_PK_Korean_Patch_Steam_1.1.7_v1.00.1.zip`**을 받으세요. `Source code (zip)`과 `Source code (tar.gz)`는 설치용 파일이 아닙니다.

지원 대상은 Steam 일본어판 「신장의 야망·신생 with 파워업키트」 **1.1.7**입니다. 게임 언어를 **일본어(Japanese)**로 설정해 주세요.

## 설치와 업데이트

1. 게임과 런처를 종료합니다.
2. 이전 패치가 적용되어 있다면 기존 패처의 **`2. 원본으로 복구`**를 먼저 실행하고 패처를 닫습니다. **v1.00 설치에 성공했던 경우에는 복구 후 Steam의 `속성 → 설치된 파일 → 게임 파일 무결성 검사`도 진행해 주세요.** 복구가 실패한 경우에도 무결성 검사를 진행합니다.
3. 새 ZIP의 파일을 모두 `NOBU16PK.exe`가 있는 게임 폴더에 풀고 덮어씁니다.
4. 새 `N16KoreanPatcher.exe`에서 `1. 패치 설정 후 적용`을 선택하고 원하는 설정으로 적용합니다.
5. `3. 현재 설치 상태 확인`에서 **`v1.00.1 적용 완료`**를 확인합니다.

**v1.00에서 위 원본 검사 오류로 설치가 중단됐고 이전 패치도 적용되어 있지 않다면, 새 ZIP을 덮어쓴 뒤 바로 적용하면 됩니다.**

`KR_PATCH_BACKUP` 폴더는 원본 복구에 필요하므로 삭제하거나 옮기지 마세요. 일반 설정은 Steam에서, 실험적 컨트롤러 지원은 `NOBU16PK_XINPUT.exe`로 실행합니다. 자세한 설치 방법과 Steam Deck 안내는 ZIP 안의 `README_KO.txt`에 있습니다.

저장 파일은 변경하지 않습니다. v1.00에 포함된 이이오 성씨 수정은 새로 시작한 게임에 적용됩니다.
