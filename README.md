# 노부나가의 야망 신생 PK 한글 패치

Windows Steam판 `NOBU16PK.exe` 일본어(JP) 리소스 경로를 사용하는 비공식
팬메이드 한글 패치입니다. 대상은 Steam JP PK v1.1.7(`Update 1.1.7`, Steam
build `18823764`)이며, Steam/런처의 게임 언어도 반드시 **Japanese**로
선택해야 합니다.

현재 공개 안정판은 [v0.11.6](https://github.com/parsifal295/nobu16-korean-patch/releases/tag/v0.11.6)입니다.
[v0.11.6 ZIP](https://github.com/parsifal295/nobu16-korean-patch/releases/download/v0.11.6/NOBU16_PK_Korean_Patch_Steam_1.1.7_v0.11.6.zip)을 받아 게임 폴더에 풉니다.
배포 ZIP SHA-256: `9BF487C431164B24CBEE5B97E3A8BB03FB77C20B6D7DD8BF9BAEC19A96BB85B1`
(374,141,982 bytes).

## 설치

1. 게임과 런처를 완전히 종료합니다.
2. ZIP의 모든 파일을 Steam 게임 폴더에 풀고 기존 리소스 덮어쓰기를 허용합니다.
   일반적인 경로는 `...\SteamLibrary\steamapps\common\NOBU16`입니다.
3. 무장 에디트 또는 가공 히메 작명에 한글을 쓸 경우 게임 폴더의
   `APPLY_STATIC_OFFICER_EDITOR_FIX.bat`를 한 번 실행합니다.
4. 완료 후에는 평소와 같이 Steam → 1.1.7 런처 → 게임으로 시작합니다.

v0.11.1 이후의 정적 설치기는 게임 실행 중에 메모리를 건드리는 helper가 아닙니다. 사용자의
정확한 원본 EXE를 로컬에서 언팩·검증하고 정적 패치 EXE로 한 번 교체합니다.
매 게임 세션마다 별도 실행 파일을 실행할 필요가 없습니다.

## v0.11.6 — NPC 표기·인물 대사·이벤트 줄바꿈·상단 헤더 누적 보정

v0.11.5와 비교해 텍스트 리소스 **6개**의 해시가 바뀌었지만, 수정 항목은
**276개**입니다. 파일 수와 수정 항목 수를 같은 의미로 쓰지 않습니다.

- Base/PK 인물 대사 레코드 **205개**, 일반 NPC 조합 조각 **46개**, 이벤트
  정적 표기·줄바꿈 셀 **25개**를 PC JP 원문과 PC EN/SC/TC 대조로 보정했습니다.
- 누적 NPC 표기에는 `덴령` → `전령`, `상사람` → `상인`, `선교모로` → `선교사`,
  `소성씨` → `시동`, `가인` → `가신`이 유지됩니다. 이번 전수 점검에서는
  `우에무라 요리카도` → `우에무라 라이렌`, `후쿠시마 마사노부` →
  `쿠시마 마사노부` 같은 고정 인명도 추가로 바로잡았습니다.
- 새로 검토한 정적 대사 20쌍은 Base/PK에 함께 반영했고, 모든 대상은 실제
  활성 글꼴 기준 3줄 이하·912px 이하입니다.
- 정적 설치기는 기존 무장 에디트·가공 히메 작명 입력 보정 9개에 상단 헤더
  가로 배치 12개를 더해 총 21개 지점을 갱신합니다. `NOBU16PK.exe`는 ZIP에
  포함하지 않으며, 필요할 때만 `APPLY_STATIC_OFFICER_EDITOR_FIX.bat`를 한 번
  실행합니다. 2048×1152에서 게임을 완전히 종료한 뒤 재실행해 확인했습니다.
- v0.11.4의 정책 효과 `%` 표기 88개(공통 39개·PK 49개)와 나머지 게임
  리소스 9개는 바이트 단위로 유지했습니다.
- 스위치판 한글은 번역·검수 기준으로 사용하지 않았습니다.

## v0.11.5 — 가공 히메 한글 작명 허용

가공 히메 작명 화면에서 한글 이름 또는 독음을 직접 입력하면 `사용할 수 없는
문자` 오류가 발생하던 Issue #62를 수정했습니다. v0.11.4의 정책 효과 퍼센트
복구와 모든 게임 리소스는 그대로 유지하고, 정적 EXE 설치기만 갱신했습니다.

- 가공 히메 작명 함수의 이름·독음 및 상속 성·성 독음 문자 검사 네 곳을 해당
  화면 안에서만 성공 처리합니다.
- 이름·독음 공란 검사, 성+이름 합산 길이 제한, 결정 버튼의 나머지 조건은
  그대로 유지됩니다.
- 이미 v0.11.0~v0.11.4의 무장 에디트 패치가 적용된 EXE도 같은 원본 백업이
  있으면 인플레이스 업그레이드합니다.
- 기존 정적 EXE 해시 `2E098ECB…1797C`는 통합 패치 해시
  `7CA2F1D5…AFDB2`로 갱신됩니다.
- Steam JP 1.1.7 실게임을 완전히 종료·재실행한 3840×2160 창 모드에서,
  사용자 확인으로 한글 입력과 결정 동작을 검증했습니다.

v0.11.5 ZIP SHA-256: `5B9CF5E245808DB532B60C27C847254F3A22987FF57A75467ED34C8C5942127D`

## v0.11.4 — 정책 효과 퍼센트 단위 복구

정책 효과 설명에서 표시되어야 할 퍼센트(`%`) 단위가 누락되는 문제를 수정했습니다.
Issue #61의 다섯 제보 항목에 국한하지 않고 전체 정책 효과를 전수 점검했습니다.

- `MSG_PK/JP/msgdata.bin`의 정책 효과 **196개**와
  `MSG/JP/strdata.bin`의 공통 정책 효과 **134개**를 확인했습니다.
- 이 중 퍼센트 리터럴이 잘못 변환된 PK **49개**와 공통 **39개**, 총 **88개** 문자열을
  해시 고정으로 복구했습니다.
- Steam JP 1.1.7 실게임을 완전히 재시작한 뒤 1920×1080에서
  `성 개발률 25%마다 부대 능력 상승` 표기의 `%` 단위가 정상 표시됨을 확인했습니다.

v0.11.4 ZIP SHA-256: `F43F0B6066EE48F398BACD6A8579EEEBFE5929837FB020703E83D3AA5899AFBB`

## v0.11.3 — NPC 표기·인물 대사·이벤트 줄바꿈 보정

v0.11.2의 검증된 Steam JP 프로필을 기준으로 NPC 명칭과 인물 대사·이벤트 텍스트를 추가로 보정했습니다.
이번 ZIP은 게임 리소스 **15개**와 설치·지원 파일 **9개**, 총 **24개**로 구성되며, v0.11.2와 비교해
텍스트 리소스 **6개**만 바뀌었습니다. 폰트·HUD·이미지 자산은 이번 변경에 포함하지 않았습니다.

- 일반 NPC 조합 구성요소와 고정 표기를 함께 보정했습니다. 대표적으로 `덴령` → `전령`,
  `상사람` → `상인`, `선교모로` → `선교사`, `소성씨` → `시동`, `가인` → `가신`을 수정했습니다.
- 현재 결합 반영분에서 Base 이벤트 정적 셀 **7개**, Base 인물 대사 레코드 **1개**, PK 인물 대사
  레코드 **33개**를 검증된 한 묶음으로 적용했습니다.
- 각 변경 전후에는 Steam JP 11개 텍스트 파일의 전체 해시와 변경 대상 레코드 범위를 확인했습니다.
  스위치판 한글은 기준으로 사용하지 않았습니다.

## v0.11.2 — PC 원문 기반 대사 품질·이벤트 줄바꿈 보정

순정 PC 일본어 원문과 PC EN/SC/TC 대조로 고확신 오역과 문법을 보정했습니다.
스위치판 한글은 기준으로 사용하지 않았습니다.

패키지는 게임 리소스 **15개**와 설치·지원 파일 **9개**, 총 **24개**로 구성됩니다.
v0.11.1과 비교하면 게임 리소스 15개 중 **10개**의 파일 해시가 바뀌었고, 설치·지원
파일 9개는 그대로입니다.

이 중 이번 버전에서 항목 단위로 집계한 대사·줄바꿈 보정은 `msggame` 텍스트 레코드
**232건**(Base 79건, PK 153건)과 PK 이벤트 대사의 정적 줄바꿈 **2건**, 총 **234건**입니다.
즉 234건은 ZIP 전체의 바이트 차이 합계가 아니라 이번 릴리즈에서 명시적으로 집계한
대사·이벤트 보정 수입니다. 동적 토큰 또는 실제 장면 확인이 필요한 항목은 포함하지
않았습니다. v0.11.1의 정적 EXE 설치기와 지원 파일은 그대로 유지합니다.

## v0.11.1 — 설치기 경로·한글 오류 출력 수정

v0.11.0의 APPLY/RESTORE 배치 파일은 `%~dp0` 끝의 `\`를 PowerShell에 전달할
때 닫는 따옴표와 충돌해 `경로에 잘못된 문자가 있습니다.` 오류로 설치가
실패할 수 있었습니다. v0.11.1은 두 배치 파일 모두 정규화 가능한 `.` 접미
경로를 전달하도록 고쳤으므로, `E:\SteamLibrary\...\NOBU16`처럼 일반적인
Steam 라이브러리 경로에서도 적용·복구가 진행됩니다.

또한 설치 PowerShell 스크립트를 UTF-8 BOM으로 배포해 Windows PowerShell
5.1에서 `실패:` 같은 한글 오류 메시지가 깨져 보이던 문제를 수정했습니다.
게임 리소스 15개와 정적 EXE의 다섯 패치 지점은 v0.11.0과 동일합니다.

## v0.11.0 — 무장 에디트 한글 성명 저장

한글 `msgdata` 성명은 일반 화면/이벤트의 `msgev` 한글 표기와 별개로,
무장 에디트의 EXE 검증에 걸렸습니다. 그래서 기존 이름을 건드리지 않아도
`성명에 사용할 수 없는 문자가 포함되어 있습니다`가 나타났습니다.

이번 정적 패치는 다음 다섯 실패 분기를 고정된 원본 바이트 확인 뒤에만
수정합니다.

- 성·이름 표기 문자 검사 2곳
- 성·이름 읽기 문자 검사 2곳
- 성명 합계 6자 제한 1곳

따라서 일반 화면의 `msgev` 한글 이름은 그대로 유지하고, 무장 에디트에서는
`가가와 미초카게`처럼 한글 표기와 비가나 읽기 필드를 가진 기존 무장도
능력치 변경 후 저장할 수 있습니다. 테스트는 1920×1080 창 모드에서 게임을
완전히 종료·재실행한 뒤 Steam → 런처 → 게임 경로로 수행했고, 능력치 변경,
`결정`, 목록 복귀 및 저장을 확인했습니다.

### 이벤트 스크립트 전수 검수·보정

이번 배포본에는 이벤트 대사 표시용 `MSG_PK/JP/msgev.bin`의 전수 검수·보정
결과를 포함했습니다. 17,916개 문자열을 대상으로 4,003개 항목을 수동 검토하고,
한글 문맥에 맞춘 줄바꿈·표시 배치 3,774건을 보정했습니다. 대화 상자는 최대
3줄·줄당 912px 기준으로 재배치했으며, 동적 인명 토큰의 폭을 예약해 이름 길이
변화에 따른 넘침을 방지합니다.

설치기는 다음의 원본만 허용합니다.

```text
NOBU16PK.exe SHA-256
29BC1ED66D27B9AEF5EB6CE3D126BA2BDBF86099E12B09615FE9F988F41E2246
```

언팩 결과와 패치 전 5개 바이트열, 최종 SHA-256까지 일치해야 교체합니다.
최종 정적 EXE SHA-256은 다음과 같습니다.

```text
2E098ECB5E4335DC264F865306B990B724EA7C242B1B9F87FFC5EE2E7191797C
```

성공하면 게임 폴더에 `NOBU16PK.exe.staticfix.original_1.1.7` 원본 백업을
남깁니다. 원복하려면 게임과 런처를 종료한 뒤
`RESTORE_ORIGINAL_NOBU16PK_EXE.bat`를 실행하면 됩니다. Steam 파일 무결성
검사나 게임 업데이트도 EXE를 원본으로 되돌릴 수 있으며, 이 경우 정확히
같은 1.1.7 원본일 때만 다시 설치할 수 있습니다.

정적 패치 EXE는 Steam 보호 래퍼와 원본 Authenticode 서명이 제거된
`NotSigned` 파일입니다. 탐색기에서 EXE를 직접 실행하지 말고 Steam 또는
1.1.7 런처에서 시작하십시오. 설치 도구는 Windows .NET Framework 4.5.2
이상이 필요하며, 포함된 Steamless v3.1.0.5 구성요소는 atom0s의 원본 그대로
CC BY-NC-ND 4.0 조건으로 제공됩니다. 자세한 고지는 배포본의
`OfficerEditorStaticFix\THIRD_PARTY_NOTICES.txt`에 있습니다.

이 변경은 한글 IME 입력이나 새 한글 이름 작성 기능을 추가하지 않습니다.
무장 에디트에서는 성명·읽기 필드를 바꾸지 말고 능력치 등 다른 항목만
수정하는 용도를 권장합니다. 문자 종류와 합산 길이 검사는 이 검증기를 쓰는
해당 편집 화면에서 완화됩니다.

EXE를 바꾸지 않는 대안은 `msgdata`의 성명·읽기만 일본어 원문으로 되돌리는
호환 모드뿐입니다. 그 경우 일반 화면의 `msgev` 한글은 유지할 수 있지만,
편집·선택 화면의 장수명은 일본어로 표시됩니다. 한글 표기를 유지한 채
통과시키는 순수 리소스 해법은 없습니다.

## v0.10.2 — 일반 이상 버튼 경로 보정

v0.10.1에서 누락된 일반 이상 실제 로딩 경로 `RES_JP/res_lang_exp.bin`을
배포본에 추가했습니다. `확인`, `거절`, `뒤로`, `허락`, `예`, `아니오` 등
버튼과 `개전` 상태는 1280×720, 1920×1080, 3840×2160에서 게임을 완전히
종료·재실행한 뒤 확인했습니다.

일본어 타이틀 로고와 튜토리얼 예시 스크린샷의 일본어는 별도 이미지
경로이므로 이 버전에 포함하지 않았습니다.

<!-- translation-progress:start -->
### v0.10.0 — 텍스트 번역 완료

`v0.10.0`은 Steam JP 1.1.7용 공개 배포본입니다. 게임 내 번역 대상 텍스트 번역을 완료했습니다.
현재는 순정 PC 일본어 원문과 PC EN/SC/TC 대조로 인물·이벤트 대사 번역 품질을 전수 감사하고 있으며, 스위치판 한글은 기준으로 사용하지 않습니다.

공식 크레딧·일본어 IME 등 번역 대상이 아닌 구조용 슬롯은 유지하고, 고확신 오역만 별도 검증 후 수정합니다.
이벤트 스크립트 줄바꿈 검수·보정과 이미지 번역은 진행 중입니다.
<!-- translation-progress:end -->

## 배포 및 기여 정책

본 한글 패치의 수정본 제작·배포와 전체 파일의 재배포는 허용하지 않습니다.
공식 배포본은 이 저장소의
[GitHub Releases](https://github.com/parsifal295/nobu16-korean-patch/releases)에서만
제공합니다.

오류 수정, 번역 개선, 누락 또는 오표기 제안은 공식 저장소의
[Issues](https://github.com/parsifal295/nobu16-korean-patch/issues) 또는
[Pull Requests](https://github.com/parsifal295/nobu16-korean-patch/pulls)를 이용해
제출해 주세요. 제안된 변경은 검토 후 공식 배포본에 반영됩니다.

공식 배포처 외의 경로에서 받은 파일은 최신성, 무결성, 호환성 및 정상 동작을
보장하지 않습니다.

## 글꼴과 권리

한글 글리프는 서울한강체 계열을 사용하며, 32px/4px 계열은 서울한강 B,
48px/6px 계열은 서울한강 EB로 렌더링했습니다.

이 프로젝트는 비공식 팬메이드 한글 패치입니다. KOEI TECMO GAMES의 공식
제품·지원물이 아니며, 게임·상표·로고·원본 데이터의 모든 권리는 KOEI
TECMO GAMES 및 각 권리자에게 있습니다. `NOBU16PK.exe` 게임 실행 파일은
배포 ZIP에 포함하지 않습니다.
