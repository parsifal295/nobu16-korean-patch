# PC 이벤트 품질 Wave 95 — 다치바나가 사후 장면

PK `MSG_PK/JP/msgev.bin`의 ID 8392–8441을 W94 후보 위에서 전수 검수한다. 벳키 아키츠라 사후, 다카하시 무네토라와 긴치요의 대화 및 후일담까지 이어지는 완결 장면이다.

- 엄격한 입력: W94 private 후보의 검증된 `msgev.bin`
- 번역 대조: 직접 PC JP 원문과 직접 PC EN/SC/TC 문맥만 사용
- 검수 결과: 50행 전체, 25행 수정·25행 유지
- 원문의 일본어 줄바꿈은 따르지 않고 한국어 의미 단위로 수동 개행
- static patch 007 레이아웃: 최대 4줄, 원본 G1N raw 48/24 advance 기준 각 줄 960px 이하
- `ceil(raw * 30 / 48)`의 30px 실효 폭은 보고용이며 두 번째 차단 기준으로 쓰지 않음
- 동적 이름은 이 장면에서만 보수 예약한다. `[bm1222]`/`[b1222]`=`다카하시 무네토라` raw 408px, `[b1221]`=`다카하시 조운` raw 312px, `[bm1730]`/`[b1730]`=`벳키 아키츠라` raw 312px. 모든 예약은 `scene_limited: true`, `runtime_proven: false`이다.
- 50행 전부를 토큰 제거가 아닌 예약 이름 치환 후의 표시 문자열·폭으로 감사한다.
- 8407·8410·8411·8421·8423·8427·8429·8432·8435·8438은 번역 문자를 바꾸지 않고 줄바꿈과 공백 배치만 의미 단위로 재구성한다. LF를 공백으로 정규화한 표시 텍스트가 W94와 일치하는지 build가 검증한다.

후보 출력은 `tmp/pc_event_tachibana_aftermath_quality_wave95_v1/candidate-final/`만 사용한다. Steam 적용, Git 작업, 네트워크 작업, 푸시, 릴리스는 이 작업에 포함하지 않는다.
