# PC 이벤트 품질 Wave 92 — 혼노지 사후 전개

대상은 PK `MSG_PK/JP/msgev.bin`의 완결된 장면 ID 7816–7849다. 혼노지의 변 이후 히데요시의 귀환·야마자키 결전·미쓰히데 최후까지를 한 장면으로 검수한다.

- 엄격한 입력: W91 사설 후보의 검증된 `msgev.bin` 프로필
- 번역 대조: 직접 PC JP 원문과 직접 PC EN/SC/TC 문맥만 사용
- 검수: 34행 전체, 26행 수정·8행 보존
- 줄바꿈: 일본어 원문의 수동 개행은 따르지 않고, 의미 단위를 보존한 한국어 수동 개행만 사용
- 레이아웃: static patch 007 기준, 최대 4줄, 원본 G1N 폭 줄당 960px 이하. `ceil(raw * 30 / 48)`의 30px 환산폭은 보고용이다.
- 동적 인명: `[b754]`와 `[bs754]`는 slot 754 기반의 이 장면 한정 보수적 예약폭이며 `runtime_proven: false`다.

후보는 `tmp/pc_event_honnouji_aftermath_quality_wave92_v1/candidate-final/`에만 생성된다. Steam 설치본·Git·원격·릴리스에는 쓰지 않는다.
