# W96 사나다·이시카와 이벤트 품질 보정

대상은 PK `MSG_PK/JP/msgev.bin`의 독립 장면 8442–8491, 총 50행이다. 입력은 `pc_event_tachibana_aftermath_quality_wave95c_v1`의 private 후보만 허용한다.

- 직접 PC JP/EN/SC/TC 네 언어 표를 50행 모두 대조한다.
- 18행은 지정된 무축약 한국어 의미 보정과 문맥형 수동 개행을 적용한다. 나머지 32행은 같은 대조·개행·예약폭 검사를 통과한 뒤 유지한다.
- ESC 색상 태그, 런타임 토큰, 종료 구조를 보존하고 태그 내부에는 개행을 넣지 않는다.
- `[b1871]`, `[bm1871]`은 `마츠다이라 모토야스`(raw 456), `[bs1871]`은 `마츠다이라`(raw 240), `[bs754]`는 `기노시타`(raw 192)로 장면 한정 예약 측정한다. 런타임 실증 여부는 모두 `false`다.
- 한 행은 최대 4줄, 각 표시 줄은 raw G1N 폭 960px 이하로 검증한다. `ceil(raw * 30 / 48)` 실효 폭은 보고용이다.
- 산출물은 이 workstream의 `tmp` 아래 private candidate 세 파일뿐이며 Steam·Git·네트워크·릴리스에는 쓰지 않는다.
