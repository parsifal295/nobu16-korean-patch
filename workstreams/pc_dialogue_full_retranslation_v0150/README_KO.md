# v0.15.0 인물 대사 전수 재번역

이 workstream은 현재 Steam PC 한글 경로의 `msggame` 두 리소스를 처음부터
의미 검토하고 새 한국어 문장으로 재번역하기 위한 **전체 작업 원장**이다.
기존의 구조·잔존 문자 감사는 번역 의미가 완료되었다는 뜻이 아니므로, 과거
검수 상태와 무관하게 모든 현재 표시 리터럴을 `unreviewed`로 시작한다.

## 범위와 기준

| 리소스 | 순정 원문 | 현재 한글 대상 | 검토 단위 |
| --- | --- | --- | --- |
| `MSG/JP/msggame.bin` | Steam PC JP | Steam PC JP-route KO | 현재 리터럴 |
| `MSG_PK/JP/msggame.bin` | Steam PC JP | Steam PC JP-route KO | 현재 리터럴 |

- 판단 자료는 순정 PC 일본어 원문, 현재 Steam PC 한글, 같은 PC의 EN/SC/TC
  문맥뿐이다. Switch 한글 및 과거 한글 번역 파일은 읽지 않는다.
- 순정 JP와 현재 KO 사이에서 과거 수정이 여러 원문 리터럴을 한 KO 리터럴로
  합친 레코드가 있다. 이 경우에도 원문 레코드 전체를 한 검토 단위에 함께 넣어
  의미를 빠뜨리지 않는다.
- `52,803`은 printable 문자가 있는 기계적 후보 수다. 여기에는 인물 대사뿐 아니라
  동적 문법 조각·시스템 알림·내부 키 가능 항목이 섞여 있으므로, 전수조사에서
  `retranslated`, `runtime_fragment_pending`, `confirmed_non_display` 중 하나로
  판정한다. `confirmed_non_display`는 근거 없이 번역하지 않는다.
- 빈 현재 슬롯은 번역 대상이 아니지만, 원문 레코드와 함께 전수 원장에 기록한다.
- Steam 설치본과 게임 리소스는 절대 쓰지 않는다. 후보 빌드도 `tmp/` 아래에서만
  만든다. Steam 적용은 별도 릴리즈 승인 직전에만 가능하다.

## 생성물

다음 명령은 모든 원문·현재 한글·PC 문맥을 `tmp/`의 **private** 큐에만 쓴다.
저장소에는 정식판 문장이나 완전한 리소스를 추가하지 않는다.

```powershell
$py = 'C:\Users\melse\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $py -B workstreams\pc_dialogue_full_retranslation_v0150\build_pc_dialogue_full_retranslation_v0150.py --write --validate
```

출력 경로는 `tmp/pc_dialogue_full_retranslation_v0150/`이다.

- `review_queue.private.v1.jsonl`: 레코드별 JP·현재 KO·PC EN/SC/TC 문맥이 있는
  실제 번역 작업 큐다.
- `target_ledger.source_free.v1.jsonl`: 원문·번역 본문 없이 좌표·해시·배치·상태만
  기록한 전수 원장이다.
- `review_batches.source_free.v1.json`: 각 배치의 대상 개수와 좌표 범위다.
- `summary.source_free.v1.json`: 입력 리소스 해시, 개수, 전수성 결과다.

모든 표시 리터럴을 검토한 뒤에는 private `review_decisions.private.v1.jsonl`에
좌표별 새 번역과 검토 상태를 넣고 다음처럼 검증한다.

```powershell
& $py -B workstreams\pc_dialogue_full_retranslation_v0150\build_pc_dialogue_full_retranslation_v0150.py --validate-decisions
& $py -B workstreams\pc_dialogue_full_retranslation_v0150\build_pc_dialogue_full_retranslation_v0150.py --build-candidate
```

진행 중인 배치 조각은 완결 전수 파일과 분리해 `--validate-partial-decisions`로
검증한다. 이 검증은 제출된 좌표의 중복·원문/현재 대상 해시·토큰·공백·가나/한자
잔류를 모두 검사하지만, 아직 제출되지 않은 좌표가 있다는 이유로 실패하지 않는다.
전체 후보 빌드는 종전과 같이 모든 표시 좌표가 승인될 때까지 차단된다.

후보 빌드는 새 번역이 모든 현재 표시 리터럴을 정확히 한 번씩 덮는지, 원문·현재
한글 기준 해시가 같은지, 런타임 토큰·ESC 태그·printf 토큰·외곽 공백이 보존됐는지
확인한 뒤 격리된 `candidate/`에만 쓴다. 줄바꿈 수를 바꾼 항목은 decision에서
`runtime_verified` 레이아웃 검토를 명시해야 한다. 이는 `msggame`의 보수적
검증 규칙이며, PK `msgev` 이벤트 대사에 관한 912px 규칙을 이 리소스에
전용하지 않는다.

번역 판단은 `STYLE_GUIDE_KO.md`와 `glossary.v1.json`을 따른다. 용어집에 없는
시대어·관직·인명·사건은 임의로 확정하지 않고 `historical_review_required`로
보류한 뒤 출처를 확인한다.

## 현재 진행

- `base_msggame_B001_S01`: 좌표 `2:80:0`~`2:94:0`의 20개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S02`: 좌표 `2:95:0`~`2:116:1`의 30개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S03`: 좌표 `2:117:0`~`2:131:2`의 33개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S04`: 좌표 `2:132:0`~`2:146:1`의 27개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S05`: 좌표 `2:147:0`~`2:162:1`의 24개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S06`: 좌표 `2:163:0`~`2:180:1`의 23개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S07`: 좌표 `2:181:0`~`2:200:2`의 52개 리터럴을 새로
  번역했다.
- `base_msggame_B001_S08`: 내부 문법·폰트 시험 블록 `1:7:0`~`1:30:2`의
  표시 리터럴 71개를 구조적 근거에 따라 `confirmed_non_display`로 판정하고
  현재 값을 보존했다. 근거는 `non_display_classification.source_free.v1.json`에
  원문 없이 기록한다.
- `base_msggame_B001_S09`~`S11`: 좌표 `2:201:0`~`2:271:2`의 표시 리터럴
  118개를 새로 번역했다. 반복 전투 대사, 역사 고유어, 인명·수치·활용 명령
  조각을 함께 검토했다.
- `base_msggame_B001_S12`~`S15`: 좌표 `2:272:0`~`2:423:0`의 표시 리터럴
  200개를 전수 분류했다. 실제 대사·런타임 조각 114개를 새로 번역하고, 명시적
  미사용 특성 더미 슬롯 86개는 구조 근거를 남긴 뒤 현재 값을 보존했다.
- `base_msggame_B001_S16`~`S19`: 좌표 `2:424:0`~`2:584:0`의 표시 리터럴
  200개를 전수 분류했다. 실제 대사·런타임 조각 141개를 새로 번역하고, 앞선
  슬롯 표에서 이어지는 명시적 미사용 특성 더미 59개는 현재 값을 보존했다.
- `base_msggame_B001_S20`~`S28`: 좌표 `2:585:0`~`4:49:0`의 표시 리터럴
  200개를 새로 번역했다. 전투·배웅 대사와 사망 보고, 저장·설정 UI를 포함하며,
  고정 문구 88개와 런타임 결합 조각 112개로 분류했다.
- `base_msggame_B001_S29`~`S38`: 좌표 `4:50:0`~`6:471:0`의 표시 리터럴
  200개를 새로 번역했다. 평정·정책·거래·장수 배치·포상·혼례·당주 교체에
  관한 인물 반응이며, 고정 대사 191개와 런타임 결합 조각 9개로 분류했다.
- `base_msggame_B001_S39`~`S48`: 좌표 `6:472:0`~`6:656:0`의 표시 리터럴
  200개를 새로 번역했다. 당주 교체·추방·군단 운용·건의 대기·공성·침공 징후·
  영지 통치 반응이며, 고정 대사 102개와 런타임 결합 조각 98개로 분류했다.
- `base_msggame_B001_S49`~`S58`: 좌표 `6:657:0`~`6:842:1`의 표시 리터럴
  200개를 새로 번역했다. 영지 통치·격무·평정중 합류·가문 성장·충성 저하·
  대인 갈등·부상 반응이며, 고정 대사 139개와 런타임 결합 조각 61개로 분류했다.
- `base_msggame_B001_S59`~`S68`: 좌표 `6:843:0`~`6:1040:0`의 표시 리터럴
  200개를 새로 번역했다. 부상·질병·정책·병량 거래·인사이동·포상·결연·은거
  반응이며, 고정 대사 196개와 런타임 결합 조각 4개로 분류했다.
- `base_msggame_B001_S69`~`S77`: 좌표 `6:1041:0`~`6:1210:1`의 표시 리터럴
  200개를 새로 번역했다. 은거·추방·군단 편제·평정 종료·논공행상·상인 거래·
  군단 지침이며, 고정 대사 137개와 런타임 결합 조각 63개로 분류했다.
- `base_msggame_B001_S78`~`S82`: 좌표 `6:1211:0`~`6:1296:2`의 표시 리터럴
  198개를 새로 번역했다. 공격 목표 선정·원군·전력 열세·병량·군비·선행 공략
  조언이며, 모두 세력·성명과 결합하는 런타임 조각으로 분류했다.
- `base_msggame_B001_S83`~`S86`: 좌표 `6:1297:0`~`6:1360:3`의 표시 리터럴
  198개를 새로 번역했다. 동맹 확대·단계적 전력 증강·병량 증산·군비 정비·
  당면 공략 목표 조언이며, 모두 세력·성명과 결합하는 런타임 조각으로 분류했다.
- `base_msggame_B001_S87`~`S92`: 좌표 `6:1361:0`~`6:1476:1`의 표시 리터럴
  198개를 새로 번역했다. 군단 편성·소속·지휘 범위·상납·임명 수락·성주 이동·
  외교 상태 문구이며, 고정 문구 58개와 런타임 결합 조각 140개로 분류했다.
- `base_msggame_B001_S93`~`S98`: 좌표 `6:1477:0`~`6:1597:1`의 표시 리터럴
  199개를 새로 번역했다. 정전·동맹·원군·친선·조정 헌금 상태 문구와 단교·종속
  보고 대사이며, 고정 문구 1개와 런타임 결합 조각 198개로 분류했다.
- `base_msggame_B001_S99`~`S103`: 좌표 `6:1598:0`~`6:1700:0`의 표시 리터럴
  200개를 새로 번역했다. 종속 관계 보고·결연·혼례·세율·정책 발령·재정난·외교
  경고·칙명 강화 문구이며, 고정 문구 34개와 런타임 결합 조각 166개로 분류했다.
  정책 부문 `普請`은 토목·건축 공사를 뜻하는 시대 용례에 따라 `토목`으로 바로잡았다.
- `base_msggame_B001_S104`~`S108`: 좌표 `6:1701:0`~`6:1890:0`의 표시 리터럴
  200개를 새로 번역했다. 칙명 강화·단교·주군 변경 경고, 외교 명령·성립 조건,
  종속·신종 제한, 교섭 재료와 자원 상한 문구이며, 고정 문구 176개와 런타임
  결합 조각 24개로 분류했다.
- `base_msggame_B001_S109`~`S113`: 좌표 `6:1891:0`~`6:2069:2`의 표시 리터럴
  199개를 새로 번역했다. 외교 교섭의 자원·영지·관직 조건, 정전·동맹·종속·단교·
  원군 제안, 지행 지급 만족도와 군단 지침 중단 보고 문구이며, 고정 문구 162개와
  런타임 결합 조각 37개로 분류했다.
- `base_msggame_B001_S114`~`S117`: 좌표 `6:2070:0`~`6:2145:1`의 표시 리터럴
  200개를 새로 번역했다. 군단 지침 중단, 잇키 진압, 공격·방어 원군 성패와 공략지
  양도 보고 문구이며, 이름·수치·일본어 활용 명령과 결합하는 런타임 조각 200개로
  분류했다. `一揆`는 한국사 학술 용례에 따라 `잇키`로 고정했다.
- `base_msggame_B001_S118`~`S123`: 좌표 `6:2146:0`~`6:2299:0`의 표시 리터럴
  200개를 새로 번역했다. 원군 철수·연장 보고, 외교 화면 안내, 우호·적대 접견과
  교섭 대가 협상 대사이며, 고정 문구 81개와 런타임 결합 조각 119개로 분류했다.
- `base_msggame_B001_S124`~`S132`: 좌표 `6:2300:0`~`6:2474:0`의 표시 리터럴
  200개를 새로 번역했다. 교섭 대가의 적정성·성공 확률·역제안·최종 조건·거절·
  분노 반응과 공략 성공 감사 대사이며, 고정 문구 144개와 런타임 결합 조각 56개로
  분류했다.
- `base_msggame_B001_S133`~`S142`: 좌표 `6:2475:0`~`6:2669:1`의 표시 리터럴
  200개를 새로 번역했다. 공성·방어 원군의 철수와 성패, 출병 중 외교의 필요성,
  친선 관계 건의 대사이며, 고정 문구 188개와 런타임 결합 조각 12개로 분류했다.
  삼십육계 제23계를 가리키는 `近攻遠交`는 정식 계책명에 맞춰 `원교근공`으로
  바로잡았다.
- `base_msggame_B001_S143`~`S152`: 좌표 `6:2670:0`~`6:2810:0`의 표시 리터럴
  200개를 새로 번역했다. 친선 관계의 구축·유지, 종속에 따른 굴욕과 재기 의지,
  적대 세력에 대한 외교적 압박, 내정 중 외교, 종속 세력의 가신 편입과 원군 반환
  연장 대사이며, 고정 문구 48개와 런타임 결합 조각 152개로 분류했다.
- `base_msggame_B001_S153`~`S162`: 좌표 `6:2811:0`~`6:2934:0`의 표시 리터럴
  200개를 새로 번역했다. 원군 반환 감사, 동맹·맹약 파기와 단교 수락·감사,
  공동 공격 약조, 종속 세력의 이탈·귀순과 배신 반응, 교섭 제안 철회 안내이며,
  고정 문구 13개와 런타임 결합 조각 187개로 분류했다.
- `base_msggame_B001_S163`~`S172`: 좌표 `6:2935:0`~`6:3071:2`의 표시 리터럴
  199개를 새로 번역했다. 조정 교섭의 대가 판단과 요구 조건 확인, 조건의 과다·적정성
  평가, 혼인 당사자의 사망·이탈에 따른 동맹 만료 안내이며, 고정 문구 79개와 런타임
  결합 조각 120개로 분류했다. 동적 인명 뒤의 조사는 `와(과)` 전환형으로 통일했다.
- `base_msggame_B001_S173`~`S182`: 좌표 `6:3072:0`~`6:3221:0`의 표시 리터럴
  200개를 새로 번역했다. 혼인 관계 단절에 따른 동맹 만료, 종속 해제와 대등 동맹,
  전봉·무장 이동, 입성·출분, 교섭 성과와 정전·화의, 종속 수락과 가문 보호, 동맹
  연장·강화·화해 및 혼인 수락 대사이며, 고정 문구 110개와 런타임 결합 조각 90개로
  분류했다. 동일한 종속 해제 원문은 구간 경계에서도 같은 한국어 구조로 통일했다.
- `base_msggame_B001_S183`~`S192`: 좌표 `6:3222:0`~`6:3371:0`의 표시 리터럴
  200개를 새로 번역했다. 혼인 수락과 인척 관계, 단교 반응, 공략·방어 원군 요청,
  조정 헌금·관직·칙명 강화 주청, 공가 접견, 관직·악명·공헌치 결과, 혼인 동맹 파기와
  군단장 추방 반응이며, 고정 문구 75개와 런타임 결합 조각 125개로 분류했다.
  `手を切る`는 `단교`, `当家`는 `우리 가문`, `貴家`는 `귀 가문`으로 통일했다.
- `base_msggame_B001_S193`~`S202`: 좌표 `6:3372:0`~`6:3478:2`의 표시 리터럴
  200개를 새로 번역했다. 군단장 추방과 후계자 지정·가독 계승 수락, 지행 무장
  일괄 임명·해임, 임기 충성 보정 경고, 연간 훈공 1위에 대한 다양한 신분·성격별
  반응이며, 고정 문구 36개와 런타임 결합 조각 164개로 분류했다. `嚢中の錐`는
  `낭중지추`로 옮겼고, `役不足`는 원뜻대로 지위가 인물의 능력에 못 미친다는
  의미를 보존했다. 동적 관직·신분 뒤 서술격은 `으로(로)서` 전환형으로 통일했다.
- `base_msggame_B001_S203`~`S212`: 좌표 `6:3479:0`~`6:3569:0`의 표시 리터럴
  199개를 새로 번역했다. 연간 훈공 1위와 승진·중용에 대한 신분·성격별 반응,
  가신의 충절과 가문 부흥 의지, 혼례 축하와 부부의 협력을 당부·수락하는 대사이며,
  고정 문구 8개와 런타임 결합 조각 191개로 분류했다. `信賞必罰`은 `신상필벌`,
  `恩義`는 문맥에 맞는 `은혜`로 옮겼고, 활용 명령에 가려진 `取れぬようでは`의
  부정 의미를 복원했다. 동적 신분 뒤 조사는 `으로(로)서` 전환형으로 통일했다.
- `base_msggame_B001_S213`~`S222`: 좌표 `6:3570:0`~`6:3675:1`의 표시 리터럴
  198개를 새로 번역했다. 혼례 수락과 부부의 보필·동행 서약, 가보의 몰수·반납·교환
  평가, 가보 수여와 취향·인연에 따른 반응 대사이며, 고정 문구 27개와 런타임 결합
  조각 171개로 분류했다. `良き伴侶`는 `좋은 반려`, `逸品`은 `일품`, `嗜好`는
  `취향`으로 옮겼다. `奪う`의 강한 `빼앗다`와 `取り上げる`의 높임 표현
  `거두어 가다`를 구분하고, `奇妙な縁`과 `不思議な縁`도 각각 `기묘한 인연`과
  `신비로운 인연`으로 나눴다.
- `base_msggame_B001_S223`~`S232`: 좌표 `6:3676:0`~`6:3787:3`의 표시 리터럴
  198개를 새로 번역했다. 가보 회수 반발과 관직 천거 감사, 동맹·정전의 잔여 기간과
  만료 경고, 외교 중개자 임명·관계 유지, 장기 협력 약정과 동맹 수락 대사이며, 고정
  문구 15개와 런타임 결합 조각 183개로 분류했다. 동적 관직의 천거 조사는
  `으로(로)`로 맞추고, `取次武将`은 `중개자 무장`, `取り持つ`는 `사이를 중개하다`,
  `臣従`은 기존 외교 용어와 같은 `신종`으로 옮겼다. 정전 종료 시에는 상대 영내에
  주둔한 병력을 철수시킨다는 방향을 보존했다.
- `base_msggame_B001_S233`~`S242`: 좌표 `6:3788:0`~`6:3910:0`의 표시 리터럴
  200개를 새로 번역했다. 외교 임무 수락과 정전 중재, 혼인·복속 수락과 단교 반응,
  시설 건설의 중단·완공, 동맹·정전 연장, 조정 관직 천거·임명, 정책·건설 지시와
  세력 관계 평가 문구이며, 고정 문구 68개와 런타임 결합 조각 132개로 분류했다.
  동일한 일본어 레코드 33개는 앞선 승인 번역과 리터럴 단위로 일치시켰다.
  `手を切る`는 `단교`, `推任`은 `천거`, `取次武将`은 `중개자 무장`, `国衆`은
  `국인중`, `役職`은 `역직`, `当家`는 `우리 가문`으로 통일했다. 문맥이 잘못
  복제된 SC/TC 레코드는 배제하고 순정 PC 일본어 의미를 따랐다.
- `base_msggame_B001_S243`~`S252`: 좌표 `6:3911:0`~`6:4019:1`의 표시 리터럴
  199개를 새로 번역했다. 세력 감정·막부 역직 수여·시설 해금과 건설·외교 요청,
  조정 사자의 관위 제안, 친선 로그, 위신과 병력에 따른 공략 평가 문구이며, 고정
  문구 21개와 런타임 결합 조각 178개로 분류했다. `役職`은 `역직`, `当家`는
  `우리 가문`, `朝廷`은 `조정`, `お上`은 문맥상 `천황`, `官位`는 `관위`,
  `従属`은 `종속`, `臣従`은 `신종`, `調略`은 `조략`으로 통일했다. 종속 요구
  문구는 순정 일본어의 결합 구조가 중의적이어서 동일 JP의 PK 영어 문맥까지 대조해
  상대가 우리 가문에 자신들의 가신이 되라고 요구하는 방향으로 확정했다. 반대 방향인
  SC/TC 문맥은 배제했다.
- `base_msggame_B001_S253`~`S262`: 좌표 `6:4020:0`~`6:4108:3`의 표시 리터럴
  197개를 새로 번역했다. 역사 인물별 공략 준비 대사, 막부 역직 수여 조건, 조정과
  외교 설명, 본거지 이전, 행군·공성·동맹·정전·친선·영내 제책 진행 로그이며, 고정
  문구 19개와 런타임 결합 조각 178개로 분류했다. `천하포무`, `독안룡`, 사쓰마계
  구호 `체스토`, 겐신의 `운은 하늘에 있고, 갑옷은 가슴에 있으며 / 공은 발에 있다`
  같은 고유 어구와 인물 말투를 보존했다. `役職`은 `역직`, `官職`은 `관직`,
  `国衆`은 `국인중`, `当家`는 `우리 가문`, `幕府役職`은 `막부 역직`, `推任`은
  `천거`, `従属`은 `종속`, `臣従`은 `신종`, `取込`은 `편입`으로 통일했다.
  PC 메뉴의 실제 신분·정책 표기에 맞춰 `조두`, `호로슈 결성`, `훈공`을 사용했고,
  일정 로그는 `완료·종료까지 N일` 구조로 통일했다.
- `base_msggame_B001_S263`~`S272`: 좌표 `6:4109:0`~`6:4195:0`의 표시 리터럴
  200개를 새로 번역했다. 영내 제책의 완료·실패 로그, 군단의 진군·공략·영지 발전·
  지원 보고, 주명 제안과 적성 조략·영내 제책 명령 설명이며, 고정 문구 3개와 런타임
  결합 조각 197개로 분류했다. `一揆`는 `잇키`, `国衆`은 `국인중`, `当家`는
  `우리 가문`, `知行地`는 `지행지`, `敵城調略`은 `적성 조략`으로 통일했다.
  `家中`은 문맥에 따라 `가신들에게`·`가신들 사이에서`로 풀어 자연스러운 한국어를
  보존했고, 반복 진군 보고와 구간 경계의 동적 조사를 결합 단위로 대조했다.
- `base_msggame_B001_S273`~`S282`: 좌표 `6:4196:0`~`6:4310:1`의 표시 리터럴
  198개를 새로 번역했다. 조략·영내 제책의 실행 조건, 조정 교섭, 정책 중단, 영주
  임명과 성 능력, 취락·시설 효과, 정책 발령과 역사 정책 표어이며, 고정 문구 27개와
  런타임 결합 조각 171개로 분류했다. 능력 축약은 실제 UI에 맞춰 `통·무·지·정`,
  `労力`은 `노동력`, `国衆`은 `국인중`, `取次武将`은 `중개자 무장`으로 통일했다.
  `천하포무`, `비사문천·파사현정`, `백만일심`, `상재전장`, `육문전·삼도천`,
  `검지`, `남만사·천주`, `자치촌`의 역사·종교 맥락을 확인하고, 동적 정책명·가문명은
  괄호와 조사로 연결해 런타임에서 이름과 한국어가 붙는 문제를 방지했다.
- `base_msggame_B001_S283`~`S292`: 좌표 `6:4311:0`~`6:4408:1`의 표시 리터럴
  199개를 새로 번역했다. 건설·원군·정전 조건, 정책 발령과 개량, 동맹에 따른 외교
  자세·신용 변화, 영지와 군다이, 성하 방침·시설, 전선 배속을 둘러싼 인물 반응이며,
  고정 문구 19개와 런타임 결합 조각 180개로 분류했다. `当家`는 `우리 가문`,
  `他家`는 `다른 가문`, `郡代`는 `군다이`, 메뉴명 `知行`은 `지행`, `城下`는
  `성하`, `石高`는 `석고`, `調略`은 `조략`으로 통일했다. 두 개의 반복 원문군은
  번역 배열까지 같게 맞췄고, segment 경계를 넘는 세력명·정책명·영지명과 활용 명령은
  raw 레코드로 재조립한 뒤 원 작성자와 다른 에이전트가 전수 교차 감사했다.
- `base_msggame_B001_S293`~`S302`: 좌표 `6:4409:0`~`6:4594:1`의 표시 리터럴
  199개를 새로 번역했다. 인물 배속과 전후방 영주 임명, 취락 장악·조략·사기·성주
  적성, 임무·건의 취소, 원군, 주가 동맹, 조정 중개, 등용과 빼내기·성째 귀순 조건
  문구이며, 고정 문구 20개와 런타임 결합 조각 179개로 분류했다. `当家`는
  `우리 가문`, `主家`는 `주가`, `城主`는 `성주`, `領主`는 `영주`, `任務`는
  `임무`, `具申`은 `건의`, `調略`은 `조략`, `所領安堵`는 `소령 안도`로
  통일했고, `城衆`은 문맥에 맞춰 `성에 있는 이들`로 풀었다. 표시되지 않는 빈
  레코드군은 decision에서 제외하고, 반복 문장 두 쌍과 segment 경계의 두 세력명
  토큰을 동일 배열·순서로 재조립했다. 초벌 작성자와 다른 에이전트가 세 구간군을
  교대로 전수 감사해 숨은 긍정·부정 활용, 인명 토큰 인용 범위와 조사 결합을 보정했다.
- `base_msggame_B001_S303`~`S312`: 좌표 `6:4595:0`~`7:252:0`의 표시 리터럴
  200개를 새로 번역했다. 귀순 조건 흥정과 야간 밀담, 등용·교섭 취소 및 가재·봉행
  임명 확인, 진군·적성 공략·국인중 회유·요새와 망루 장악 조언, 적장 표적 선정과
  포박 후 처우, 성격별 등용 수락 대사이며, 고정 문구 120개와 런타임 결합 조각
  80개로 분류했다. `家宰`는 `가재`, `従属大名家宰`는 `종속 다이묘 가재`,
  `奉行`은 `봉행`, `引抜`은 `빼내기`, `譲与`는 실제 UI 동작에 맞춰 `양도`,
  `国衆`은 `국인중`, `物見櫓`는 `망루`, `首級`은 `수급`, `捕縛`은 `포박`,
  `処断`은 `처단`, `処遇`는 `처우`, `解放`은 `해방`으로 통일했다. 병법의
  `졸속`과 `신속`을 구분하고, 노부나가를 비롯한 등용 수락 화자의 말투 차이와
  가문명·인물명·국인중 이름 토큰의 인용 범위를 보존했다. 동일한 빈 레코드 24개는
  decision에서 제외했으며, 초벌 작성자와 다른 에이전트가 세 구간군을 교대로 전수
  감사했다.
- `base_msggame_B001_S313`~`S322`: 좌표 `7:253:0`~`7:402:0`의 표시 리터럴
  200개를 새로 번역했다. 포박된 무장의 등용 수락·거절과 해방 감사, 등용·귀순
  성공 로그, 처단 전 최후의 말과 가보 입수 로그, 일괄 해방 확인 및 반응 대사이며,
  고정 문구 106개와 런타임 결합 조각 94개로 분류했다. `貴家`는 `귀 가문`,
  `当家`는 `우리 가문`, `主家`는 `주가`, `登用`은 `등용`, 포박 후 세력 편입을
  뜻하는 `降る`는 `귀순`, `捕縛`은 `포박`, `解放`은 `해방`, `処断`은 `처단`,
  `家宝`는 `가보`, `処遇`는 `처우`, `矜持`는 `긍지`로 통일했다. 동일 원문
  레코드 세 쌍은 각각 `거절한다`, `거절하겠습니다`, `후회는 없사옵니다`로 정확히
  일치시켰고, segment를 가로지르는 레코드 `7:266`, `7:274`, `7:383`의 토큰·조사
  결합 순서를 재조립해 확인했다. 세 에이전트가 초벌 담당 구간을 서로 바꿔 전수 교차
  감사했으며, 줄바꿈 전용 비표시 리터럴 두 개는 decision에서 제외했다.
- `base_msggame_B001_S323`~`S332`: 좌표 `7:403:0`~`7:577:0`의 표시 리터럴
  200개를 새로 번역했다. 포박된 무장의 해방에 대한 감사·도발과 해방 결과 로그,
  패전 뒤 퇴각하는 성격별 대사, 적 부대 격파·격퇴 성과 대사, 부대 궤멸에 따른 종속도 하락,
  협력 세력의 철수·결별과 적대 회피 대사이며, 고정 문구 91개와 런타임 결합 조각
  109개로 분류했다. `解放`은 `해방`, `壊滅`은 선행 승인 용례와 같은 `궤멸`,
  `兵糧`은 `병량`, `従属度`는 `종속도`, `軍略`은 `군략`, 전투 문맥의 `我が武`는
  `내 무용`, `武功`은 `무공`, `武運`은 `무운`, `袂を分かつ`는 `결별하다`로
  통일했다. 문어적으로 중의적인 `再び相見えん`은 동일 일본어 배열의 PC PK 영어·
  SC·TC를 함께 대조해 다시 만나지 않는 영별 의미로 확정했고, 용을 자칭하는 화자의
  개성도 보존했다. B047 175개 레코드 중 97개는 동일 일본어 배열의 PK 영어 문맥까지
  대조했다. 동일 원문 `7:476`·`7:482`와 `7:528`·`7:532`, 선행 승인과 같은
  `7:415`·`7:392`를 각각 정확히 일치시켰고, segment를 가로지르는 `7:550`의
  부대명·세력명 토큰 순서와 조사를 재조립했다. 세 에이전트가 초벌 담당을 바꿔 전수
  교차 감사했다.
- `base_msggame_B001_S333`~`S342`: 좌표 `7:578:0`~`7:703:0`의 표시 리터럴
  199개를 새로 번역했다. 협력 세력과 싸울 수 없어 원군에서 이탈하는 성격별 대사,
  병량 고갈과 탈주·부대 붕괴, 거점과 본거지의 상실·탈환, 세력별 함락 로그와 공략
  성공 대사이며, 고정 문구 12개와 런타임 결합 조각 187개로 분류했다. `殿`은 문맥과
  화자에 따라 `님`·`공`, `兵糧`은 `병량`, `本拠`는 `본거지`, `入城`은 `입성`으로
  통일하고, `陥落`·`落とす`·`攻め落とす`의 함락과 `奪う`의 탈취, `制す`의 제압,
  `手中に収める`의 획득을 구분했다. 고전 완료 감탄 `奪ったり`는 선택형이 아닌
  `빼앗았도다!`로 옮겼고, `7:643`은 숨은 활용 opcode가 완성할 `잊지 않` 어간을
  보존했다. B048 126개 레코드 중 55개는 동일 일본어 배열과 비어 있지 않은 PK 영어
  문맥까지 대조했다. 동일 원문 `7:620`·`7:634`, `7:625`·`7:635`, `7:668`·
  `7:678`을 각각 정확히 일치시키고, 선행 승인 `7:600`·`6:3102`와 `7:601`·
  `6:3103`의 입성 로그도 재사용했다. segment를 가로지르는 `7:619`, `7:646`,
  `7:656`, `7:666`, `7:676`의 거점 토큰·인용부호·조사를 재조립했으며, 세
  에이전트가 초벌 담당을 바꿔 전수 교차 감사했다.
- `base_msggame_B001_S343`~`S352`: 좌표 `7:704:0`~`7:818:0`의 표시 리터럴
  199개를 새로 번역했다. 성격별 거점·적 본거지 공략 성공, 세력 멸망 직전의 인물
  대사, 본거지 이전에 따른 통치·지휘 범위와 군단 소속 변경, 공략 목표 해제 로그이며,
  고정 문구 22개와 런타임 결합 조각 177개로 분류했다. `敵本拠`는 `적 본거지`,
  `本拠移転`은 `본거지 이전`, `大名軍団`은 `다이묘 군단`, `準備城`은 `준비성`,
  `攻略目標`는 `공략 목표`로 통일했다. `武田の名`·`毛利の名`은 각각 다케다·모리의
  `이름`, `盛者必衰`는 정형어 `성자필쇠`, `祟る`는 죽어서도 저주한다는 원혼의
  의미로 옮겼다. 원문과 반대였던 PK 영어의 모리 대사는 채택하지 않았다. B049 115개
  레코드 중 49개는 동일 일본어 배열과 비어 있지 않은 PK 영어 문맥까지 대조했다.
  `7:751`~`7:790`의 적 본거지 대사 40개는 `7:686`~`7:725`의 일반 거점 대사와
  의미·화자 어조를 정확히 맞췄다. `7:792`는 숨은 활용 opcode 앞뒤를 재조립해 영지를
  지켜 주지 못했다는 부정 의미를 복원했고, `7:801`의 이름난 성 외 추가 성 수와
  `7:802`·`7:805`의 이름난 성을 포함한 총 성 수를 구분했다. 동일 일본어 배열
  `7:797`·`7:798`은 정확히 일치시켰으며, segment를 가로지르는 `7:791`과 `7:808`의
  세력·거점 토큰, 원군 요청 주체와 소속 방향을 재조립했다. 세 에이전트가 초벌 담당을
  바꿔 전수 교차 감사했다.
- `base_msggame_B001_S353`~`S362`: 좌표 `7:819:0`~`7:925:0`의 표시 리터럴
  200개를 새로 번역했다. 세력 쇠퇴에 따른 출분과 등용 기회, 원군 거절, 인물·병력의
  침공 보고, 전국 다이묘의 역사적 이명과 숙적 반응, 배수진 결의, 승전과 무명 선양
  대사이며, 고정 문구 55개와 런타임 결합 조각 145개로 분류했다. `尾張の大うつけ`는
  `오와리의 멍청이`, `海道一の弓取り`는 궁수가 아닌 `도카이도 제일의 무사`,
  `鬼若子`는 장종아부 원친의 이명 `오니와코`, `天下静謐`는 `천하의 평온`으로
  통일했다. `仏敵`·`不倶戴天`·`悪鬼羅刹`은 각각 `불적`·`불구대천`·`악귀나찰`로,
  `雌雄を決す`는 `자웅을 가리다`로 옮겼다. B050 107개 레코드 중 82개는 동일
  일본어 배열과 비어 있지 않은 PK 영어 문맥까지 대조했으며, 교토에 설 깃발을 한 번의
  싸움으로 밝힌다는 `7:862`는 반대 의미인 PK 영어를 채택하지 않았다. 동일 일본어·raw
  반복군 `7:831`~`7:841`, `7:884`~`7:890`, `7:891`~`7:897`은 번역 배열까지
  정확히 일치시켰다. `7:819`과 `7:831`~`7:841`은 숨은 활용 opcode 앞에서 각각
  `여기까지라`, `침공해 와` 어간을 보존했고, segment를 가로지르는 `7:883`과
  `7:889`의 세력·인물·병력·거점 토큰 순서를 재조립했다. 세 에이전트가 초벌 담당을
  바꿔 전수 교차 감사했다.
- `base_msggame_B001_S363`~`S372`: 좌표 `7:926:0`~`7:1059:0`의 표시 리터럴
  200개를 새로 번역했다. 승전·무명 선양 구호, 위풍 발생과 주변 세력의 관계 악화,
  승패에 대한 제3세력·국인중·종속 세력의 성격별 반응, 주변 국인중의 종속도 변화,
  합전 지휘 확인, 적 세력 전력 평가 대사이며, 고정 문구 18개와 런타임 결합 조각
  182개로 분류했다. `威風`은 `위풍`, `当家`는 `우리 가문`, `周辺国衆`은
  `주변 국인중`, `従属度`는 `종속도`, `武名`은 문맥에 따라 `용맹한 명성`,
  `謀神`은 `모신`, `盛者必衰`는 `성자필쇠`, `雌雄を決する`는 `자웅을 가리다`,
  `武田騎馬隊`는 `다케다 기마대`로 통일했다. 여불위 고사의 `奇貨居くべし`는
  `이런 기화는 미리 손에 넣어 두는 법이지`로 옮겼고, `三河武士の精強さ`는
  `미카와 무사의 강인함`으로 풀었다. B051 134개 레코드 중 133개가 PK와 동일한
  일본어 raw 배열이었으며, 그중 43개는 비어 있지 않은 PK 영어 문맥까지 대조했다.
  `武略は武士の嘘`를 실책으로 바꾼 PK 영어와 승패 방향을 뒤집은 `7:978`의 PK
  영어는 채택하지 않았다. `7:951`의 실제 패배, `7:1005`의 이미 이룬 승리,
  `7:1042`의 `쓸어 버려 주마` 의미를 복원하고, `7:1013`~`7:1059`의 성격별
  전력 평가 47개는 상대 세력 토큰과 조사 방향을 전수 확인했다. 내부·선행 동일
  raw 반복은 없었고, segment를 가로지르는 `7:963`, `7:974`, `7:1004`의
  승자·패자·숨은 화자 토큰과 인용부호를 재조립했다. 세 에이전트가 초벌 담당을 바꿔
  전수 교차 감사했다.
- `base_msggame_B001_S373`~`S382`: 좌표 `7:1060:0`~`7:1246:0`의 표시 리터럴
  200개를 새로 번역했다. 대등한 적과의 교전 판단, 다른 가문에 원군을 청하는 성격별
  진언, 압도적인 대군과 결사 항전하거나 정전을 청하는 대사이며, 200개 전부 상대
  세력명 런타임 토큰과 결합되는 조각으로 분류했다. `他家`는 `다른 가문`, `援軍`은
  `원군`, `停戦`은 `정전`, `武名`은 `용맹한 명성`, `越後の龍`은 `에치고의 용`,
  `独眼竜`은 `독안룡`, `風林火山の兵法`은 `풍림화산의 병법`으로 통일했다.
  `武者震い`는 공포가 아니라 `무사의 피가 끓는` 전율로, `草刈り場`는 문자 그대로의
  풀밭이 아니라 강자가 손쉽게 유린할 `먹잇감`으로 옮겼다. `臥薪嘗胆`·`捲土重来`는
  각각 `와신상담`·`권토중래`, `死中の活`은 `사지에서 활로를 찾다`로 보존했다.
  겐신의 벽서로 전하는 `戦は死なんとすれば生きるもの`와 `運は天にあり`는 각각
  `죽을 각오로 임해야 사는 법`, `운은 하늘에 있는 법`으로, 『삼략』에서 유래한
  `柔よく剛を制す`는 `부드러움이 강함을 제압하는 이치`로 옮겼다. B052 187개
  레코드는 모두 PK와 동일한 일본어 raw 배열이었으나 대응 PK 영어 표시 문맥은 전혀
  없었다. SC·TC 표시 문맥도 각각 네 레코드뿐이고 동일 raw 반복 `7:1133`·`7:1213`에
  서로 다른 축약문이 잘못 대응되어 있어, 순정 일본어를 우선했다. 동일 raw 반복군
  `7:1133`~`7:1166`과 `7:1213`~`7:1246`의 34쌍은 번역 리터럴 배열과 UTF-16LE
  바이트까지 일치시켰다. 상대 세력 토큰이 선행하는 174개 레코드와 두 리터럴 사이에
  놓인 13개 레코드를 구분하고, 중간 토큰형은 인용부호로 한국어 수식어·조사 경계를
  분리했다. segment를 가로지르는 `7:1155`도 합성했으며, 세 에이전트가 초벌 담당을
  바꿔 전수 교차 감사했다.
- `base_msggame_B001_S383`~`S392`: 좌표 `7:1247:0`~`7:1435:0`의 표시 리터럴
  200개를 새로 번역했다. 수비가 허술한 성을 소수 병력으로 선점하거나 쉬운 상대에게도
  전군을 동원하는 진언, 대등한 성을 선제공격하거나 주변 병력을 집중해 공략하는 대사이며,
  200개 전부 대상 성·상대 세력 런타임 토큰 또는 인라인 `Cs1.CsName`과 결합되는
  조각으로 분류했다. `手薄`는 `수비가 허술하다`, `寡兵`·`小勢`는 `적은 병력`·`소수
  병력`, `勿怪の幸い`는 `뜻밖의 행운`, `毘沙門天`은 `비사문천`, `機先を制する`는
  `기선을 제압하다`, `伯仲`은 `백중지세`, `集中運用`은 `집중 운용`으로 통일했다.
  `韋駄天`은 한국 불교의 정착 표기인 [`위태천`](https://encykorea.aks.ac.kr/Article/E0033399)으로
  옮겼고, `人は城`은 [고후시가 소개하는 신겐의 국정 이념](https://www.city.kofu.yamanashi.jp/shingenkou-no-machi/wartactics.html)에
  맞춰 `사람은 성`으로 보존했다. `7:1379`의 순정 일본어 `拙速は巧遅に如かず`는
  선제공격 문맥과 반대로 [일본한자능력검정협회의 정문](https://www.kanken.or.jp/kanken/challenge/level04/180806.html)
  `巧遅は拙速に如かず`를 뒤집은 오기다. 『손자』 「작전」의
  [`兵聞拙速、未睹巧之久也`](https://ctext.org/text.pl?if=en&node=20919&show=parallel)와도 대조해
  `더디게 잘하기보다 서툴러도 빠른 편이 낫다`는 의도된 뜻으로 복원했다. 189개
  레코드는 모두 PK와 동일한 일본어 raw 배열이었으나 대응 PK 영어 표시 문맥은 없었고,
  SC·TC 표시 문맥도 각각 다섯 레코드뿐이었다. 그중 동일 raw 반복에 잘못 대응된 축약
  문맥은 배제하고 순정 일본어를 우선했다. 선행 승인본과 같은 46개 레코드·51개 리터럴은
  번역 배열까지 그대로 재사용했으며, 내부 반복 `7:1247`~`7:1252`와
  `7:1327`~`7:1332`도 일치시켰다. 대상 성 토큰 선행 136개, 상대 세력 토큰 선행
  41개, 중간 토큰형 11개, 인라인 토큰형 한 개의 raw 경계를 보존하고, 중간 토큰형은
  인용부호·관형절·삽입 구문으로 한국어 조사와 띄어쓰기를 재조립했다. 세 에이전트가
  초벌 담당을 바꿔 전수 교차 감사했다.
- `base_msggame_B001_S393`~`S402`: 좌표 `7:1436:0`~`7:1615:1`의 표시 리터럴
  199개를 새로 번역했다. 전력이 대등한 성에 주변 병력을 집중하거나 다른 가문의 원군과
  함께 공격하는 진언, 성하·인근의 군을 먼저 제압해 성을 고립·약체화하고 지리적 이점을
  얻는 공략책, 우호 가문과의 관계를 끊고 성을 노리는 대사이며, 199개 전부 대상 성·우호
  가문 런타임 토큰과 결합되는 조각으로 분류했다. `他家`는 `다른 가문`, `援軍`은 `원군`,
  `郡`은 `군`, `城下の郡`은 `성하의 군`, `地の利`는 `지리적 이점`, `伯仲`은
  `백중세`로 통일했다. `裸城`은 [성하를 잃어 고립되거나 방어 시설을 잃은 성이라는 사전
  의미](https://kotobank.jp/word/%E8%A3%B8%E5%9F%8E-601680)에 맞춰 `고립시켜 무방비한 성`으로
  옮겼다. `将を射んとせばまず馬を射よ`·`人を射んとせばまず馬を射よ`는 상대가
  의지하는 것부터 공략한다는 [두보 유래의 비유](https://www.kanjipedia.jp/kotoba/0003689400)와
  원전 구절 [`射人先射馬`](https://dict.variants.moe.edu.tw/dictView.jsp?educode=A01060&powerMode=2)을
  대조해 `장수를/사람을 쏘려거든 먼저 말을 쏘라`로 보존했다. 180개 레코드는 모두 PK의
  레코드 번호 `+40`과 동일한 일본어 raw 배열이었으나 대응 PK 영어 표시 문맥은 없었고,
  SC·TC 표시 문맥도 각각 다섯 레코드뿐이어서 순정 일본어를 우선했다. 내부 반복
  `7:1453`~`7:1492`와 `7:1533`~`7:1572`의 40쌍은 번역 리터럴 배열과 UTF-16LE
  바이트까지 일치시켰다. 대상 성 토큰 선행 161개, 대상 성 토큰 중간 16개, 대상 성
  선행·우호 가문 중간 이중 토큰형 두 개, 우호 가문 선행·대상 성 중간 이중 토큰형 한
  개의 raw 경계를 보존하고, 중간·이중 토큰형은 삽입 구문·관형격·조사를 재조립했다.
  세 에이전트가 초벌 담당을 바꿔 전수 교차 감사했다.
- `base_msggame_B001_S403`~`S412`: 좌표 `7:1616:0`~`7:1721:0`의 표시 리터럴
  200개를 새로 번역했다. 우호 가문과의 관계·지침을 재검토하거나 단교하고 수비가 약한
  성을 빼앗는 진언, 공성 전에 병량을 더 사거나 조달하라는 성격별 대사이며, 우호 가문·
  대상 성 토큰 결합 조각 171개와 고정 대사 29개로 분류했다. `指針`은 `지침`,
  `友好指針`은 `우호 지침`, `手切り`는 `단교`, `手薄`는 `수비가 허술하다`,
  탈취 문맥의 `頂く`·`頂戴`·`切り取る`는 `차지하다`·`빼앗다`로 통일했다. `兵糧`은
  `병량`, `城攻め`는 `공성`, `調達`은 `조달`, `購入`은 `구입`, `買い足す`는
  `더 사 두다`로 구분했다. 런타임 opcode를 고정된 이름 종류로 간주하지 않고 각 레코드의
  일본어 조사·서술어로 역할을 판정했으며, `7:1639`는 같은 범위에서 유일하게 두 슬롯의
  성·가문 역할이 뒤바뀌어 `<성>을 빼앗기 위해 <가문>과의 관계를 끊다`로 재조립했다.
  원문의 혼합형 오기 `言えぬなでしょう`도 병량이 충분하다고는 할 수 없다는 의도된
  진술로 복원했다. 106개 레코드는 모두 PK의 레코드 번호 `+40`과 동일한 일본어 raw
  배열이었으나 대응 PK 영어 표시 문맥은 없었고, SC·TC 표시 문맥도 각각 두 레코드뿐인
  축약 보조문이어서 순정 일본어를 우선했다. 내부·선행 동일 raw 반복은 없었다. raw
  경계는 `025032→026432` 37개, `026432→025032` 18개, 리터럴 선행 뒤
  `026432→025032` 세 개, 리터럴 선행 뒤 `025032→026432` 19개, 무토큰 29개를
  보존했다. 비표시 공백 리터럴 다섯 개는 decision에서 제외하고 그대로 보존했으며,
  세 에이전트가 초벌 담당을 바꿔 전수 교차 감사했다.
- `base_msggame_B001_S413`~`S422`: 좌표 `7:1722:0`~`7:1896:0`의 표시 리터럴
  200개를 새로 번역했다. 공성 전 병량 부족 경고, 입성 불가에 따른 진로 변경, 공략·
  방어·구원 중단, 성 함락과 귀성, 군 제압·선무 완료 및 우군이 된 목표에서 철수하는
  성격별 대사이며, 성·제압 대상·귀환 성 런타임 토큰과 결합되는 조각 185개와 고정 대사
  15개로 분류했다. `兵糧`은 `병량`, `入城`은 `입성`, `転進`은 전진이 아니라 `진로를
  바꾸다`, `攻略`은 `공략`, `防衛`는 `방어`, `救援`은 `구원`, `帰城`은 `귀성`,
  `制圧`은 `제압`, `宣撫`는 `선무`, `武名`은 `무명`으로 통일했다. `7:1856`의
  `侵掠すること火の如し`는 『손자』 「군쟁」의
  [`侵掠如火`](https://ctext.org/art-of-war/maneuvering)를 대조해 `침략하기를 불같이`로
  보존했다. 175개 레코드는 모두 PK의 레코드 번호 `+40`과 동일한 일본어 raw
  배열이었으며, Base SC·TC와 PK EN·SC·TC에는 각각 100개 레코드의 표시 문맥이 있어
  보조 대조하되 순정 일본어를 우선했다. 동일 raw인 `7:1804`·`7:1809`는 번역까지
  일치시켰다. 서로 다른 동적 opcode를 쓰지만 일본어 리터럴 배열이 같은
  `7:1813`~`7:1832`와 `7:1833`~`7:1852`의 20쌍도 번역 리터럴 배열과 UTF-16LE
  바이트를 정확히 일치시켰다. `7:1821`·`7:1841`·`7:1861`의 미해명
  `014301000000` 문법 조각은 삭제하거나 추정하지 않고 리터럴 경계와 함께 보존했다.
  raw 경계는 무토큰 15개, 대상 성 토큰 선행 90개·중간 9개, 대상 성 토큰 선행 뒤
  불투명 조각 한 개, 제압 대상 토큰 중간 한 개·선행 45개, 제압 대상 토큰 선행 뒤
  불투명 조각 두 개, 제압 대상 뒤 귀환 성 이중 토큰형 12개를 보존했다. 비표시 공백
  리터럴은 없었고, 세 에이전트가 초벌 담당을 바꿔 전수 교차 감사했다.
- `base_msggame_B001_S423`~`S432`: 좌표 `7:1897:0`~`7:2023:1`의 표시 리터럴
  200개를 새로 번역했다. 목표 부대가 사라진 뒤 철수·귀성·입성하는 대사, 방해 적을
  격파하고 성 공략·방어를 재개하는 대사, 협격을 막으러 이동하거나 성을 공격하는 적
  부대를 요격하는 대사, 전세가 불리해 목적지까지 후퇴하는 대사이며, 성·목적지·적 부대
  런타임 토큰과 결합되는 조각 184개와 고정 대사 16개로 분류했다. `帰城`은 `귀성`,
  `入城`은 `입성`, `攻略`은 `공략`, `守備`는 `수비`, `防衛`는 `방어`, `挟撃`은
  프로젝트 UI 용어인 `협격`, `迎撃`은 `요격`, `殲滅`은 `섬멸`, `攻囲軍`은
  `공성군`, `要地`는 `요지`, `旗色が悪い`는 `전세가 불리하다`로 통일했다.
  `7:1997`의 `虎が山を出た隙に`는 SC 문맥이 명시한 `调虎离山`과
  [『삼십육계』 제15계 `調虎離山`](https://ctext.org/wiki.pl?chapter=477324&if=en)을
  대조해 `조호이산의 계책인가`로 옮겼다. Base `7:1897`~`7:1993` 97개 레코드는
  PK의 레코드 번호 `+40`과 일본어 raw가 일치했다. PK 전용 `7:2034` 한 건이 삽입된
  뒤에는 대응 위치가 이동하므로 Base `7:1994`~`7:2023` 30개는 PK `+41`과 정확히
  일치시켰다. Base SC·TC와 정확 대응한 PK EN·SC·TC에는 각각 77개 레코드의 표시
  문맥이 있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw인 `7:1945`·`7:1950`,
  `7:1970`·`7:1973` 두 쌍은 번역 리터럴 배열과 UTF-16LE 바이트까지 일치시켰고,
  선행 승인본과 일치하는 raw는 없었다. raw 경계는 무토큰 16개, 성 토큰 중간 41개·
  선행 30개, 목적지 토큰 선행 6개·중간 13개·후행 terminator 결합 한 개, 성→적 부대
  이중 토큰형 10개, 적 부대→성 이중 토큰형 다섯 개, 리터럴 선행 뒤 적 부대→성 두 개·
  성→적 부대 한 개, 성→적 부대 뒤 terminator 결합 두 개를 보존했다. `7:1978:1`과
  `7:1980:1`의 개행 전용 비표시 리터럴 두 개는 decision에서 제외하고 그대로 보존했으며,
  세 에이전트가 초벌 담당을 바꿔 전수 교차 감사했다.
- `base_msggame_B001_S433`~`S442`: 좌표 `7:2024:0`~`7:2199:0`의 표시 리터럴
  200개를 새로 번역했다. 열세일 때 목적지로 후퇴하거나 추격을 멈추고 성 방어로
  돌아가는 대사, 병량이 적은 성을 포위하거나 아군 본거지의 위기·병량 부족·적 후원군
  접근 때문에 공성을 서두르는 대사, 작은 성 또는 장기 포위보다 강공을 택하는 성격별
  대사이며, 성 토큰 결합 조각 114개와 고정 대사 86개로 분류했다. `劣勢`는 `열세`,
  `追撃`은 `추격`, `駆逐`는 `격퇴`, `守備`는 `수비`, `防衛`는 `방어`, `強攻`은
  `강공`, `包囲`는 `포위`, `兵糧`은 `병량`, `落城`·`陥落`은 `함락`, `本拠`는
  `본거지`, `速攻`은 `속공`, `後詰`은 `후원군`, `力攻め`·`力押し`는 `힘으로
  밀어붙이다`, `小城`은 `작은 성`, `一気呵成`은 `단숨에 몰아치다`로 통일했다.
  `7:2037`의 `今は六分の勝ちでよい`는 후대 군학서 『甲陽軍鑑』 「品第四十一」에서 신겐의
  가르침으로 전하는 [6~7할의 승리를 온전한 승리로 보는 대목](https://ja.wikisource.org/wiki/%E7%94%B2%E9%99%BD%E8%BB%8D%E9%91%91/%E5%93%81%E7%AC%AC%E5%9B%9B%E5%8D%81%E4%B8%80)과
  대조해 `지금은 6할의 승리면 족하다`로 옮겼다. 무주어인 `7:2179`의 `蓄えは十分`은
  적의 비축으로 보는 PK 영어와 아군 우세로 보는 Base SC가 엇갈리므로, 순정 일본어가
  확정하지 않은 주체를 보강하지 않고 `비축분은 충분하다`로 보존했다. Base
  `7:2024`~`7:2053` 30개 레코드는 PK `+41`과 일본어 raw가 일치했다. PK 전용
  `7:2095` 한 건 뒤에는 대응 위치가 이동하므로 Base `7:2054`~`7:2199` 146개는
  PK `+42`와 정확히 일치시켰다. Base SC·TC와 대응 PK EN·SC·TC에는 각각 106개
  레코드의 표시 문맥이 있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw인
  `7:2025`·`7:2032`, `7:2083`·`7:2091`, `7:2176`·`7:2196` 세 쌍은 번역 리터럴
  배열과 UTF-16LE 바이트까지 일치시켰고, 선행 승인본과 일치하는 raw는 없었다. 실제
  pristine raw에서 다시 산출한 경계는 `026432` 토큰 중간 24개·선행 26개,
  `025032` 토큰 선행 20개, `026532` 토큰 선행 20개, 무토큰 86개이며, 최초 구간
  추정값 대신 이 실바이트를 검증 기준으로 사용했다. 비표시 공백 리터럴은 없었고,
  세 에이전트가 구간별 초벌 번역과 다른 담당 구간의 전수 교차 감사를 수행했다.
- `base_msggame_B001_S443`~`S452`: 좌표 `7:2200:0`~`7:2373:0`의 표시 리터럴
  200개를 새로 번역했다. 후원군을 활용해 강공하거나 아군 피해를 피하려 포위하는 대사,
  본래 아군 영지·군의 탈환, 공략 중단과 철수, 주변 제압·부대 배치·포위 완료 뒤의 공성,
  지정 지역에서 포위 완성을 기다리거나 기회를 틈타 영지를 제압하는 성격별 대사이며,
  영지·군·성·대기 지역 토큰 결합 조각 109개와 고정 대사 91개로 분류했다. `後詰`은
  `후원군`, `強攻`은 `강공`, `包囲`는 `포위`, `力攻め`·`力押し`는 `힘으로
  밀어붙이다`, `領地`는 `영지`, `郡`은 `군`, `奪還`은 `탈환`, `撤退`는 `철수`,
  `退却`은 `퇴각`, `城攻め`·`攻城`은 `공성`, `制圧`은 `제압`, `足場`는 `발판`,
  영토 문맥의 `切り取る`는 `빼앗다`로 통일했다. `裸城`은 선행 감사 기준과 같이 성하를
  잃고 고립된 상태를 뜻하므로 `성을 고립시켜 무방비하게 만들다`로 옮겼다.
  `7:2365`의 `先んずれば人を制す`는 [『史記』 「項羽本紀」의 선제 행동과 제압에
  관한 대목](https://ctext.org/shiji/xiang-yu-ben-ji/zh)을 대조해 `선수를 치면 남을
  제압할 수 있습니다`로 보존했다. `7:2371`의 `抜け駆け功名`은 단순한 선봉이 아니라
  [진을 먼저 빠져나가 남보다 앞서 적진에 들어가 공을 세운다는 사전 의미](https://kotobank.jp/word/%E6%8A%9C%E3%81%91%E9%A7%86%E3%81%91-594134)에
  맞춰 `남보다 앞서 공을 세우는 것이 전장의 꽃`으로 바로잡았다. `7:2306`의 표면상
  모호한 `待つべきではないか`는 뒤의 공성 개시 명령과 PK EN·SC 문맥이 같은 방향을
  가리키므로 `더 기다려서는 안 된다`로 복원했다. Base 174개 레코드의 대응 위치는
  모두 PK `+42`다. 그중 173개는 일본어 raw가 정확히 일치했고, `7:2309`만 Base의
  `攻城戦`과 PK의 `城攻め`가 같은 의미를 서로 다른 말로 표현하므로 순정 Base 문구를
  우선해 `공성전`으로 옮겼다. Base SC·TC와 대응 PK EN·SC·TC에는 각각 102개
  레코드의 표시 문맥이 있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw인
  `7:2206`·`7:2213`은 번역 리터럴 배열과 UTF-16LE 바이트까지 일치시켰고, 선행
  승인본과 일치하는 raw는 없었다. 실제 pristine raw 경계는 무토큰 91개,
  `026432` 토큰 중간 세 개·선행 20개, `029632` 토큰 중간 23개·선행 37개를
  보존했다. 비표시 공백 리터럴은 없었고, 세 에이전트가 구간별 초벌 번역과 다른 담당
  구간의 전수 교차 감사를 수행했다.
- `base_msggame_B001_S453`~`S462`: 좌표 `7:2374:0`~`7:2517:0`의 144개 레코드에
  든 표시 리터럴 200개를 새로 번역했다. 목표 거점 방어, 출진 대상·병력·병량·경로·지원
  선택 UI, 죽음을 앞둔 장수의 대사와 적장 격파 대사이며, 고정 대사 77개와 동적 토큰·
  활용 명령 결합 조각 123개로 분류했다. `兵糧`은 `병량`, `腰兵糧`은 `휴대 병량`,
  `冥土`·`黄泉路`·`泉下`는 문맥에 따라 `저승`·`황천길`·`황천`, `首`·`首級`는
  적장 격파 문맥에서 `적장의 수급`, `武家の務め`는 `무가의 책무`, `末期`는
  `임종`, `天晴`는 `훌륭하다` 계열로 통일했다. `7:2452`의 `夢のまた夢`은
  [도요토미 히데요시 자필 사세 와카에 전하는 `ゆめの又ゆめ`](https://japanheritage.jp/ja/osaka/heritage/toyotomi-hideyoshi-death-poem)와
  같은 표현임을 확인해 `꿈속의 또 꿈이로구나……`로 옮겼다. 다만 이 어구의 연관성만
  참고했으며, 게임 화자나 대사의 사료적 진위를 히데요시의 와카와 동일시하지 않았다.
  `7:2498`의 `泉下に沈めい`는 `황천으로 가라앉거라!`, `7:2508`의 `戦ぶり`는
  상대의 싸움을 칭찬하는 `참으로 훌륭히 싸웠도다!`, `7:2515`·`7:2517`의
  `首`·`首級`는 `적장의 수급`으로 복원했다. Base→PK 대응은 `7:2374`~`7:2411`
  `+42`, `7:2412`~`7:2416` `+44`, `7:2417`~`7:2422` `+45`, `7:2423`~
  `7:2448` `+46`, `7:2449`~`7:2517` `+66`이다. 144개 중 일본어 리터럴 배열
  143개가 정확히 일치했고, `7:2416`은 PK `7:2460`과 대응하지만 Base 세 리터럴·
  PK 두 리터럴로 경계가 달라 Base의 세 조각과 불투명 코드를 그대로 보존했다. raw
  전체가 일치한 대응은 101개다. Base SC·TC와 대응 PK EN·SC·TC에는 각 112개
  레코드의 표시 문맥이 있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw인
  `7:2397`·`7:2401`, `7:2398`·`7:2402`, `7:2403`~`7:2406`,
  `7:2408`·`7:2409`, `7:2457`·`7:2497`, `7:2469`·`7:2484`는 번역 리터럴
  배열과 재패킹 바이트까지 일치시켰고 선행 승인본과 겹치는 raw는 없었다. raw 경계는
  `029632` 선행 거점 토큰형 20개, 무토큰 77개, `0143`·`0250`·`0264`·`026e`·
  `0296`·`0232`·`023c`가 결합된 복합 시스템 레코드 47개로 나뉜다. 표시 번역의
  줄 수는 한 줄 127개·두 줄 72개·세 줄 한 개이며, 개행 또는 빈 문자열뿐인
  `7:2394:1`, `7:2411:1`, `7:2414:2`, `7:2444:4`, `7:2445:1` 다섯 개는
  decision에서 제외하고 보존했다. 세 에이전트가 구간별 초벌 번역과 서로 다른 담당
  구간의 전수 교차 감사를 수행했다.
- `base_msggame_B001_S463`~`S472`: 좌표 `7:2518:0`~`7:2670:1`의 153개 레코드에
  든 표시 리터럴 200개를 새로 번역했다. 적장 격파와 수급, 일번창과 전공 제일,
  퇴로 차단·요지 확보로 세운 공, 자신의 활약을 평가하거나 다른 장수의 공을 인정·질투·
  칭찬하는 성격별 대사이며, 고정 대사 63개와 이름·활용 명령 결합 조각 137개로
  분류했다. `首`·`手柄首`는 적장 격파 문맥에서 `수급`, `戦功第一`는 `전공 제일`,
  `手柄`는 `공`, `武勲`은 `무훈`, `武勇`은 `무용`, `退路`·`退き口`는 `퇴로`,
  `要地`·`要所`·`要の地`는 `요지`로 구분했다. `一番槍`은 단순한 전공 순위가 아니라
  [전장에서 가장 먼저 적진에 창을 찔러 넣는 공이라는 사전 의미](https://kotobank.jp/word/%E4%B8%80%E7%95%AA%E6%A7%8D-433621)와
  프로젝트의 기존 UI·전기 표기에 맞춰 `일번창`으로 통일했다. `7:2530`의 `この信長`,
  `7:2536`의 `独眼竜`, `7:2592`의 `三河武士`, `7:2595`의 `毛利`, `7:2596`의
  `奥州の独眼竜`은 원문이 명시한 `이 노부나가`·`독안룡`·`미카와 무사`·`모리`·
  `오슈의 독안룡`만 보존하고 별도의 인물·일화를 덧붙이지 않았다. `7:2593`의
  `五、六分の勝ちを旨としておる`은 앞서 검증한 후대 군학서 『甲陽軍鑑』의
  [6~7할의 승리를 온전한 승리로 보는 대목](https://ja.wikisource.org/wiki/%E7%94%B2%E9%99%BD%E8%BB%8D%E9%91%91/%E5%93%81%E7%AC%AC%E5%9B%9B%E5%8D%81%E4%B8%80)과
  같은 모티프에 맞춰 `5할, 6할의 승리를 신조로 삼고 있거늘`로 옮기되 실제 화자의
  확정 발언으로 단정하지 않았다. `7:2619`의 `心に自慢なき時は人の善を知る`는
  흔히 우에스기 가훈 열여섯 조항으로 전하지만, [국립국회도서관 레퍼런스 사례가
  우에스기 관계 사료집에서는 확인하지 못하고 『名将言行録』을 바탕으로 한 전승 경로를
  기록한 점](https://crd.ndl.go.jp/reference/entry/reference/show?asc=desc&fi=2_2&id=1000075463&kg1=99&kg2=2&kg3=6&kg4=13&kg5=1&kw1=%E8%B0%B7%E6%B2%A2%E6%B0%B8%E4%B8%80&kw_b=1&kw_lk1=1&ldtl=1&mcmd=25&page=ref_view&schup_btn=%E6%A4%9C%E7%B4%A2&st=score)을
  고려해 직접 사료 인용으로 단정하지 않고 `마음에 자만이 없어야 남의 장점을 안다`로
  복원했다. Base 153개 레코드는 모두 PK `+66`과 일본어 raw·리터럴 배열이 정확히
  일치했다. Base SC·TC와 대응 PK EN·SC·TC에는 각 92개 레코드의 표시 문맥이 있어
  보조 대조하되 순정 일본어를 우선했다. 구간 내부 동일 raw와 선행 승인본에 겹치는 raw는
  모두 없었다. raw 경계는 무토큰 63개와 동적 레코드 90개로 나뉘며, 동적 레코드에는
  `024733` 40개, `024735` 21개, `014301` 32개 레코드·34회, `014304` 한 개,
  `7:2529`의 `025032`·`024633` 결합 한 개가 서로 일부 중첩된다. 표시 번역의 줄 수는
  한 줄 70개·두 줄 75개·세 줄 55개이고 비표시 리터럴은 없었다. 세 에이전트가 구간별
  초벌 번역과 서로 다른 담당 구간의 전수 교차 감사를 수행했다.
- `base_msggame_B001_S473`~`S482`: 좌표 `7:2671:0`~`7:2795:1`의 125개 레코드에
  든 표시 리터럴 198개를 새로 번역했다. 다른 장수의 전공을 축하하거나 자신의 전과·
  무훈을 평가하는 성격별 대사, 자율 행동 설정 튜토리얼, 귀성·잇키 진압·항복·귀순·
  외교 관계·종속도·충성·원군 시스템 조각이며, 고정 대사 41개와 동적 토큰 결합 조각
  157개로 분류했다. `戦功第一`는 `전공 제일`, `手柄`·`功`은 `공`, `戦果`는
  `전과`, `働き`는 `활약`, `武勲`은 `무훈`, `武功`은 `무공`, `采配`는 `지휘`,
  `一揆`는 `잇키`, `自律行動`은 `자율 행동`, `鎮圧`은 `진압`, `当家`는
  `우리 가문`, `国衆`은 `국인중`, `従属`은 `종속`, `威風`은 `위풍`으로
  통일했다. `7:2697`의 `御旗楯無`는 [다케다 가문이 `御旗・楯無`라 부르며 신성시한
  깃발과 갑옷이라는 야마나시현 문화재 설명](https://www.pref.yamanashi.jp/bunka/bunkazaihogo/bunkazai_data/yamanashinobunkazai_rc0002.html)과
  프로젝트의 기존 이벤트 표기에 맞춰 `미하타와 다테나시의 가호`로 옮겼다.
  `7:2699`의 `手柄は足にあり`는 앞서 번역한 겐신 계열 문구와 맞춰 `공은 발에 있다`,
  `7:2700`의 `百万一心`은 [모리 모토나리의 일치단결 가르침으로 전해진다는
  아키타카타시 설명](https://www.akitakata.jp/ja/hakubutsukan/assets/mouri/)에 맞춰
  `백만일심`으로 보존하되 두 문구 모두 게임 밖 실제 발언의 진위를 새로 단정하지 않았다.
  `7:2701`의 `いぶし銀の働き`는 원문에 없는 노련함을 더하지 않고 `화려하진 않아도
  묵직한 활약`으로 옮겼다. Base→PK 의미 대응은 `7:2671`~`7:2771` `+66`,
  `7:2772`~`7:2773` `+67`, `7:2774`~`7:2780` `+69`, `7:2781`~`7:2786`
  `+71`, `7:2787`~`7:2795` `+78`이다. 일본어 리터럴 배열은 121개, raw 전체는
  95개가 정확히 일치했다. `7:2772`·`7:2773`과 `7:2779`·`7:2780`은 Base와 PK의
  귀순·이탈 문구 및 리터럴 경계가 달라 Base의 세력·성·군 토큰 순서와 귀순 방향을
  우선했다. Base SC·TC에는 각 97개, 대응 PK EN에는 94개, PK SC·TC에는 각 97개
  레코드의 표시 문맥이 있어 보조 대조했다. 동일 raw인 `7:2739`~`7:2743`·
  `7:2754`~`7:2758` 열 개, `7:2744`~`7:2748` 다섯 개, `7:2759`~`7:2763`
  다섯 개는 번역 리터럴 배열과 재패킹 바이트까지 일치시켰고 선행 승인본과 겹치는 raw는
  없었다. raw 경계는 40개 형태로 나뉘며, 무토큰 41개와 `0143`·`0232`·`024733`·
  `024735`·`025032`·`028c32`·`029632` 등이 결합된 동적 레코드 84개의 Base 골격을
  그대로 보존했다. 표시 번역의 줄 수는 한 줄 95개·두 줄 99개·세 줄 두 개·네 줄 한 개·
  다섯 줄 한 개이고 비표시 리터럴은 없었다. 세 에이전트가 구간별 초벌 번역과 서로 다른
  담당 구간의 전수 교차 감사를 수행했다.
- `base_msggame_B001_S483`~`S492`: 좌표 `7:2796:0`~`8:250:2`의 148개 레코드에
  든 표시 리터럴 200개를 새로 번역했다. 공격 준비·합력·전승 보고, 병량·금전·군마·철포
  생산 증가, 영지 발전과 지행 수여, 전투·공성 외침, 목적지 진입과 세력 종속·배반을
  다루는 성격별 대사 및 시스템 조각이며, 고정 대사 84개와 동적 토큰 결합 조각 116개로
  분류했다. `兵糧`은 `병량`, `金銭収入`은 `금전 수입`, `軍馬`는 `군마`, `鉄砲`는
  `철포`, `領地`는 `영지`, `領民`은 `영민`, `知行`은 `지행`, `陣`은 `진`으로
  통일했다. `飴と鞭`는 선행 승인본과 맞춰 `당근과 채찍`, 갚아야 할 의리를 뜻하는
  `恩義`는 `은의`, `粉骨砕身`은 `분골쇄신`으로 옮겼다. 공격 대상 문맥의 `攻める`·
  `攻め取る`·`落とす`·`奪う`·`制す`·`切り取る`는 각각 `공격`·`공략`·`함락`·
  `빼앗다`·`제압`·`차지`로 구분하고, 목적지 문맥의 `入る`·`入城`·`乗り込む`는
  `들어가다`·`입성`·`쳐들어가다`로 구분했다. `7:2796`의 현행 과거형은 원문의 출진
  의지에 맞춰 `쳐부수고 돌아오겠다` 계열로 바로잡았다. `8:241`의 `見限る`는 단순
  배반을 뜻하는 `寝返る`와 구별해 `버리다`, `8:242`의 `頼る`는 `의탁하다`,
  `8:244`의 `鞍替え`는 `가담하다`로 옮겼다. 동적 이름 두 개가 맞붙거나 조사가
  불분명해지는 24개 레코드에는 `「동적명」` 경계를 적용했고, `8:246:1`의 선행 전각
  공백 한 자는 보호 시그니처로 그대로 보존했다. Base→PK 의미 대응은 `7:2796`~
  `7:2805` `+78`, `7:2806` `+80`, `8:114`~`8:250` `+6`이며 일본어 리터럴
  배열은 148개 전부, raw 전체는 141개가 정확히 일치했다. Base SC·TC에는 각 95개,
  대응 PK EN·SC·TC에는 각 98개 레코드의 표시 문맥이 있어 보조 대조하되 순정 일본어를
  우선했다. 동일 raw는 비표시 빈 리터럴인 `7:2806`·`8:114` 한 쌍뿐이며 두 레코드는
  decision에서 제외해 원본 바이트를 보존했고, 선행 승인본과 겹치는 raw는 없었다.
  raw 골격은 26개 형태로 나뉘며 무토큰 86개 레코드와 동적 토큰 결합 62개 레코드의
  비문자열 골격을 그대로 보존했다. 표시 번역의 줄 수는 한 줄 103개·두 줄 96개·세 줄
  한 개다. 세 에이전트가 구간별 초벌 번역과 서로 다른 담당 구간의 전수 교차 감사를
  수행했고, 전체 200개를 합친 메모리 재패킹에서 선택 146개 레코드의 골격과 비대상
  19,006개 레코드의 바이트가 정확히 보존됨을 다시 확인했다.
- `base_msggame_B001_S493`~`S503`: 좌표 `8:251:0`~`8:328:2`의 78개 레코드에
  든 표시 리터럴 199개를 새로 번역했다. 군단의 병량 고갈, 풍작·흉작, 가뭄·홍수·
  태풍과 사전 대비에 따른 피해 경감을 다루는 성격별 보고 및 시스템 조각이며, 고정 대사
  두 개와 동적 토큰 결합 조각 197개로 분류했다. `兵糧枯渇`은 `병량 고갈`, `豊作`은
  `풍작`, `凶作`은 `흉작`, `年貢米`는 `연공미`, `干ばつ`는 `가뭄`, `洪水`는
  `홍수`, `台風`은 `태풍`, `民忠`은 `민충`, `郡特性`은 `군 특성`, `治水`는
  `치수`, `城下施設`은 `성하 시설`, `集落`은 `취락`으로 통일했다. `8:266`의
  `備えあれば憂いなし`는 [사전이 설명하는 `備え有れば患い無し`·`有備無患` 계열의
  고사성어](https://kotobank.jp/word/%E5%82%99%E3%81%88%E3%81%82%E3%82%8C%E3%81%B0%E6%86%82%E3%81%84%E3%81%AA%E3%81%97-2236698)와
  뜻을 함께 살려 `유비무환의 덕으로`로 옮기되 게임 밖 특정 인물의 실제 발언으로는
  귀속하지 않았다. `田の衰え`는 자연재해 문맥에 맞춰 `논의 황폐화`, `差配`는 전투
  지휘가 아닌 재해 `대처`로 구분했다. 동적 피해 지역 목록과 군 수가 결합되는 알림은
  `을(를) 비롯해 피해를 입은 군은 총…개` 순서로 통일하고, 보고형·추량형 활용 토큰
  앞에는 `상태`·`모양` 등의 명사를 배치해 Base의 활용 경계를 보존했다. Base→PK 의미
  대응은 `8:251`~`8:274` `+6`, `8:275`~`8:316` `+8`, `8:317`~`8:328`
  `+10`이며 일본어 리터럴 배열은 72개, raw 전체는 아홉 개가 정확히 일치했다.
  `8:285`~`8:289`와 `8:296`은 Base와 PK의 문장·리터럴 경계가 달라 Base 원문의
  토큰 순서와 활용 골격을 우선했다. Base SC·TC와 대응 PK EN·SC·TC에는 각 74개
  레코드의 표시 문맥이 있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw인
  `8:258`~`8:262`, `8:268`~`8:272`, `8:280`~`8:284`, `8:290`~`8:294`,
  `8:300`~`8:304`, `8:310`~`8:314`, `8:322`~`8:326`의 일곱 묶음은 각각
  번역 리터럴 배열과 재패킹 바이트까지 일치시켰고 선행 승인본과 겹치는 raw는 없었다.
  raw 골격은 45개 형태로 나뉘며 무토큰 고정 레코드 두 개와 동적 토큰 결합 레코드
  76개의 비문자열 골격을 그대로 보존했다. 표시 번역의 줄 수는 한 줄 66개·두 줄
  112개·세 줄 21개다. 개행만 든 비표시 리터럴 `8:307:1`은 decision에서 제외해
  원본을 보존했다. 세 에이전트가 구간별 초벌 번역과 서로 다른 담당 구간의 전수 교차
  감사를 수행했고, 전체 199개를 합친 메모리 재패킹에서 선택 78개 레코드의 골격과
  비대상 19,074개 레코드의 바이트가 정확히 보존됨을 다시 확인했다.
- `base_msggame_B001_S504`~`S513`: 좌표 `8:329:0`~`8:434:1`의 106개 레코드에
  든 표시 리터럴 199개를 새로 번역했다. 태풍 피해와 사전 대비, 치안·군 특성·인구·
  성하 시설 피해, 재해·전국 통일·저장 알림, 영지 개발과 전봉 수락·거절, 잇키,
  출생·양자·원복·출가를 다루는 성격별 보고와 시스템 조각이며, 무토큰 고정 대사
  31개와 동적 토큰 결합 조각 168개가 든 75개 레코드로 분류했다. `逃散`은
  [중세·근세 농민이 토지를 떠나 도망하거나 흩어지는 일을 뜻한다는 사전
  설명](https://kotobank.jp/word/%E9%80%83%E6%95%A3-97841)에 맞춰 현대 한국어의
  파산 의미인 `도산`을 피하고 `백성들이 도망쳐 흩어져`로 풀었다. `元服`은
  [일본의 대표적인 성인식이라는 사전 설명](https://kotobank.jp/word/%E5%85%83%E6%9C%8D-61021)과
  프로젝트 용어집에 맞춰 `원복`으로 통일했다. `子`는 별도 딸 출생 대사와 구분해
  `아들`, `配下`는 `휘하`, `一門`은 `일문`, `養子`·`養女`는 `양자`·`양녀`,
  `姫`는 `공주`로 옮겼다. Base→PK 의미 대응은 `8:329`~`8:338` `+10`,
  `8:339`~`8:434` `+12`이며 일본어 리터럴 배열은 106개 전부, raw 전체는
  78개가 정확히 일치했다. Base SC·TC와 대응 PK EN·SC·TC에는 각각 101개
  레코드의 표시 문맥이 있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw인
  `8:332`~`8:336`, `8:344`~`8:348`, `8:349`~`8:353`, `8:354`~`8:358`,
  `8:415`·`8:416`, `8:423`·`8:424`, `8:430`·`8:431`, `8:432`·`8:433`은
  각 묶음의 번역 리터럴 배열과 재패킹 바이트를 일치시켰다. 이 가운데 `8:332`~
  `8:336`은 선행 승인 `8:310`~`8:314`, `8:372`는 선행 승인 `8:163`과
  원문 raw가 같아 승인 번역과 후보 레코드 바이트를 그대로 재사용했다. raw 골격은
  40개 형태로 나뉘며 모든 보호 시그니처와 원래 줄 수를 보존했다. 표시 번역의 줄 수는
  한 줄 115개·두 줄 78개·세 줄 6개이고 비표시 리터럴은 없었다. 세 에이전트가
  구간별 초벌 번역과 서로 다른 담당 구간의 전수 교차 감사를 수행했으며, 전체 199개를
  합친 메모리 재패킹에서 선택 106개 레코드의 골격과 비대상 19,046개 레코드의
  바이트가 정확히 보존되고 역오버레이가 원본과 완전히 같음을 다시 확인했다.
- `base_msggame_B001_S514`~`S522`: 좌표 `8:435:0`~`8:597:0`의 163개 레코드에
  든 표시 리터럴 199개를 새로 번역했다. 공주·당주·군단장·가신·성주의 사망 알림,
  여성의 출가와 성격별 임종 대사, 능력·군 특성 성장 알림, 지행 배정에 대한 불만과
  가증에 대한 감사, 전봉 때 오래 다스린 땅과 작별하는 대사를 다루며, 무토큰 고정
  대사 116개와 동적 토큰 결합 조각 83개가 든 47개 레코드로 분류했다. `落飾`은
  [높은 신분의 사람이 머리를 깎고 불문에 드는 일을 뜻한다는 사전
  설명](https://kotobank.jp/word/%E8%90%BD%E9%A3%BE-655886)에 맞춰 `머리를 깎고
  출가`로 풀고, 전사자의 `菩提を弔う`는 `명복을 빌다`로 옮겼다. `天下布武`는
  프로젝트 용어집과 [노부나가가 기후를 거점으로 삼은 뒤 그 인장을 사용하기 시작했다는
  기후시 역사박물관 설명](https://www.rekihaku.gifu.gifu.jp/exhibition/)에 맞춰
  `천하포무`로 보존하되 성격별 임종 문구를 게임 밖 실제 유언으로 새로 귀속하지 않았다.
  `知行`은 [중세·근세의 토지·재산 지배와 가신에게 지급한 토지·봉록을 뜻한다는 사전
  설명](https://kotobank.jp/word/%E7%9F%A5%E8%A1%8C-95952)에 맞춰 `지행`,
  `知行高`는 `지행고`, `加増`은 [영지나 녹봉을 더 늘리는 일이라는 사전
  설명](https://kotobank.jp/word/%E5%8A%A0%E5%A2%97-463071)에 맞춰 `가증`으로
  통일했다. `家中`은 가족이 아닌 `가신단`, `我が武`는 `나의 무용`으로 구분했다.
  `多々益々弁ず`는 [일이 많을수록 능숙히 해내거나 많을수록 좋다는 고사성어
  설명](https://kotobank.jp/word/%E5%A4%9A%E3%80%85%E7%9B%8A%E3%80%85%E5%BC%81%E3%81%9A-560636)에
  따라 `다다익선`, `良禽は木を選ぶ`는 [현명한 신하는 섬길 주군을 잘 고른다는 비유라는
  사전 설명](https://kotobank.jp/word/%E8%89%AF%E7%A6%BD%E3%81%AF%E6%9C%A8%E3%82%92%E9%81%B8%E3%81%B6%E3%81%A7%E4%BD%8F%E3%82%80-659172)을
  살려 `좋은 새는 나무를 가려 앉는다`로 옮겼다. Base→PK 의미 대응은 전 구간
  `+12`이며 일본어 리터럴 배열과 raw 전체가 163개 전부 정확히 일치했다. Base SC·
  TC에는 각 79개, 대응 PK EN에는 75개, PK SC·TC에는 각 79개 레코드의 표시 문맥이
  있어 보조 대조하되 순정 일본어를 우선했다. 동일 raw 묶음과 선행 승인본에 겹치는
  raw는 없었다. raw 골격은 14개 형태로 나뉘며 모든 보호 시그니처와 원래 줄 수를
  보존했다. 표시 번역의 줄 수는 한 줄 58개·두 줄 135개·세 줄 6개이고 비표시
  리터럴은 없었다. 세 에이전트가 구간별 초벌 번역과 서로 다른 담당 구간의 전수 교차
  감사를 수행했으며, 전체 199개를 합친 메모리 재패킹에서 선택 163개 레코드의 골격과
  비대상 18,989개 레코드의 바이트가 정확히 보존되고 역오버레이가 원본과 완전히 같음을
  다시 확인했다.
- `base_msggame_B001_S523`~`S531`: 좌표 `8:598:0`~`8:777:0`의 180개 레코드에
  든 표시 리터럴 200개를 새로 번역했다. 오래 다스린 영지에서 물러나는 회고, 지행
  회수와 감봉에 대한 불만·수용, 영민의 지지와 민심 안정, 잇키 선동 위험, 태풍에
  대비한 치수 상태, 봄 경관과 농사 대사를 다루며, 무토큰 고정 대사 115개와 동적
  토큰 결합 조각 85개가 든 65개 레코드로 분류했다. `知行`·`知行高`·`知行地`는
  `지행`·`지행고`·`지행지`, `召し上げ`는 지행의 `회수`나 `거두다`, `我が武`는
  `내 무용`, `参陣`은 `전장에 나서다`로 구분했다. `家中`은 [전국시대 이후
  가신단의 총칭으로 정착했다는 사전 설명](https://kotobank.jp/word/%E5%AE%B6%E4%B8%AD-45092)에
  맞춰 `가신단`으로 옮겼다. `一所懸命の地`는 [중세 무사가 생활의 기반인 한 영지를
  목숨 걸고 지킨다는 뜻이라는 사전 설명](https://kotobank.jp/word/%E4%B8%80%E6%89%80%E6%87%B8%E5%91%BD-434245)에
  맞춰 현대의 단순한 `열심히`가 아니라 `목숨 걸고 지켜 온 영지`로 풀었다.
  `一揆`는 `잇키`, `調略`은 `조략`, `煽動`은 `선동`, `民心`은 `민심`, `治水`는
  `치수`, `堤`는 `제방`, `水害`는 `수해`로 통일했다. `春に百花あり`는 `봄에는
  백화가 있고`로 문학적 호흡을 보존하고, `山笑う`는 [봄 산의 초목이 돋아 밝아진
  모습을 나타내는 계절어라는 사전 설명](https://kotobank.jp/word/%E5%B1%B1%E7%AC%91%E3%81%86-649054)에
  맞춰 `「산이 웃는다」는 말이 참이로구나`로 옮겼다. Base→PK 의미 대응은 전 구간
  `+12`이며 일본어 리터럴 배열과 raw 전체가 180개 전부 정확히 일치했다. Base SC·
  TC와 대응 PK EN·SC·TC에는 각각 59개 레코드의 표시 문맥이 있어 보조 대조하되 순정
  일본어를 우선했다. 범위 내부 동일 raw와 선행 승인본에 겹치는 raw는 없었다.
  `8:669`는 아직 검토하지 않은 `9:2762`와 raw가 같지만 승인 번역이 없으므로 직접
  재사용하지 않았다. raw 골격은 12개 형태로 나뉘며 모든 보호 시그니처와 원래 줄 수를
  보존했다. 표시 번역의 줄 수는 한 줄 34개·두 줄 165개·세 줄 한 개이고 비표시
  리터럴은 없었다. 세 에이전트가 구간별 초벌 번역과 서로 다른 담당 구간의 전수 교차
  감사를 수행했으며, 전체 200개를 합친 메모리 재패킹에서 선택 180개 레코드의 골격과
  비대상 18,972개 레코드의 바이트가 정확히 보존되고 역오버레이가 원본과 완전히 같음을
  다시 확인했다.
- 생성 검토 큐 `base_msggame-B008`~`base_msggame-B067`은 표시 대상 197개짜리 한
  배치, 198개짜리 일곱 배치, 199개짜리 열다섯 배치, 200개짜리 서른일곱 배치 전부가
  분류되어 의미 범위 검토가 완료됐다.
  작성용 segment의 `B001` 명칭과 생성 큐 batch 번호는 서로 다른 식별자다.
- 누적 승인·분류는 11,968개다. 실제 재번역 3,629개, 런타임 조각 8,123개,
  비표시 내부 데이터 216개로 구성된다.
- 정적 재번역 3,629개와 비표시 보존 216개, 합계 3,845개는 후보 반영 자격을 갖췄다.
  동적 인명·세력·수치·활용 명령과 결합되는 8,123개는 의미 검증을 통과했으며
  실게임 조사 결합 검수가 남았다.
- 진행 수치는 `progress.source_free.v1.json`에 원문 없이 기록한다.
