# W77 코마키·나가쿠테 이벤트 대사 품질·개행 검수

이 작업 스트림은 PK 이벤트 ID `8235`~`8302`의 68행을 전수 검수한다. 입력은 반드시 온디스크 W76 후보
`tmp/pc_event_amago_quality_wave76_v1/candidate-final/MSG_PK/JP/msgev.bin`이며, 해당 파일 범위와 압축·원시 프로필이
고정값과 일치하지 않으면 후보를 만들지 않는다.

변경 26행은 다음과 같다.

`8236, 8237, 8238, 8239, 8240, 8241, 8243, 8249, 8252, 8256, 8259, 8263, 8268, 8273, 8278, 8281, 8282, 8283, 8284, 8287, 8289, 8292, 8293, 8296, 8300, 8302`

나머지 42행은 직접 PC JP 및 PC EN/SC/TC 문맥 대조 뒤 유지한다. 일본어 원문의 개행은 이식하지 않으며, 한국어 개행은 의미 단위로만 배치한다. 문장 축약·정보 삭제·인명 변경은 하지 않는다.

각 줄은 원본 G1N 전각 48px·반각 24px으로 측정해 raw 960px 이하, 최대 4줄로 제한한다. 감사 보고서에는 표시 문자열, raw 폭, `ceil(raw * 30 / 48)` 실효 폭, 전각/반각 문자 수 및 초과 여부를 기록한다. 색상 태그·런타임 토큰·종료 구조는 보존하며 태그 내부에는 개행을 넣지 않는다.

`[b1871]`, `[bm1871]`, `[bs1871]`, `[b754]`, `[bs754]`, `[b1976]`는 이 장면에만 한정한 보수적 폭 예약으로 측정한다. 이는 W76 이름 슬롯을 근거로 한 레이아웃 예약일 뿐, 실제 게임에서 해당 장면의 토큰 표시를 확인한 증거는 아니다. 감사 산출물은 이 점을 `runtime_proven: false`로 명시한다.

후보 산출물은 `tmp/pc_event_komaki_nagakute_quality_wave77_v1/candidate-final/` 아래에만 생성한다. Steam 적용, Git 커밋·푸시, 릴리스는 이 작업 범위에 포함하지 않는다.
