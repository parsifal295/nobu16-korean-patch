# PC 이벤트 품질 Wave 93 — 모리 모토나리 꽃놀이 장면

PK `MSG_PK/JP/msgev.bin`의 완결 장면 ID 5977–5999를 W92 후보 위에서 검수한다. 5976은 직전 장면의 끝이며, 6000은 별도 사카모토성/아케치 장면이므로 범위에 넣지 않는다.

- 엄격한 입력: W92 private 후보의 검증된 `msgev.bin`
- 번역 대조: 직접 PC JP 원문 및 직접 PC EN/SC/TC 문맥만 사용
- 검수 결과: 23행 전체, 15행 수정·8행 유지
- 일본어 원문의 줄바꿈은 따르지 않고 한국어 의미 단위로 수동 개행
- static patch 007 레이아웃: 최대 4줄, 원본 G1N raw 48/24 advance 기준 각 줄 960px 이하
- `ceil(raw * 30 / 48)`의 30px 실효 폭은 보고용이며 두 번째 차단 기준으로 쓰지 않음
- 이 장면에는 런타임 인명 토큰이 없으며 예약폭도 사용하지 않음

후보 출력은 `tmp/pc_event_mori_motonari_quality_wave93_v1/candidate-final/`만 사용한다. Steam 적용, 커밋, 푸시, 릴리스는 이 작업에 포함하지 않는다.
