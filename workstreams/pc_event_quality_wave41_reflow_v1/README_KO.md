# Wave 41 — PC PK 이벤트 문맥형 줄바꿈 보정

이 workstream은 PK 이벤트 6셀의 수동 줄바꿈과, 기존 줄바꿈에 가려져 있던 어절
공백만 보정하는 private 후보다. 문장을 무조건 한 줄로 합치지 않고, 의미 단위와
인용문 경계를 유지한 채 각 문장을 3줄 이내·한 줄 912px 이하로 다시 배치한다.

- `5558`: `은빛 메기꼬리 투구`를 한 명사구로 유지한다.
- `5832`: 관용구 `겉과 속`을 붙여 읽히게 한다.
- `7083`: `두 병위`, `양 병위`의 고유 호칭을 각각 한 덩어리로 둔다.
- `7579`: 두 인용문을 `」 「`로 명확히 구분한다.
- `7801`: 두 인용문과 `배신과 밀회`의 어절 경계를 유지한다.
- `7845`: `낙오 무사 사냥꾼`을 복합 명사로 보존한다.

모든 변경은 공백·개행만 바꾼다. PC JP/EN/SC/TC 셀 해시, ESC/런타임/printf 등
비레이아웃 제어 시퀀스, 비공백 문자 순서를 검증한다. 그러나 실제 게임의 이벤트
표시·자동 줄바꿈 상태는 별도이므로, 여섯 행 모두 실게임 화면 QA가 끝나기 전까지
Steam 적용·커밋·릴리즈 후보가 아니다.

`tmp/pc_event_quality_wave41_reflow_v1/candidate/` 아래 private 후보만 생성하며,
Steam 적용·Git·네트워크·릴리즈 기능은 없다.
