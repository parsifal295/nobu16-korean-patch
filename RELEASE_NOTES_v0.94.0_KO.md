# v0.94.0 릴리스 노트

v0.94.0은 Steam 일본어판 PK 1.1.7, Steam build 18823764용 한국어 패치
정식 릴리스입니다. v0.93.0까지의 번역·컨트롤러·Steam Deck·화면 표시 보정을 모두
포함하며, 지행 호출 화면의 좌·우 인물 목록 간격, 외교 제안 문구, 전법 설명의
조각 사이 공백과 가와고에성 주변 지명을 추가로 교정합니다.

등용 화면의 `전체승인`, 고해상도 전투 시작 `개전` 이미지, 기본판·PK 네비게이션 메뉴 휠과 공용 일반 버튼도 함께 교정합니다.

## 외교 제안 문구 — #118

- 외교 관계를 맺자는 제안의 결론이 `안이옵니다`로 표시되던 문구를
  `좋은 방안이옵니다`로 교정했습니다.
- 공통 종결 어미는 바꾸지 않고 `MSG_PK/JP/msggame.bin`의 해당 문장 조각 하나만
  수정해 다른 대사에 영향을 주지 않도록 했습니다.

## 지행 호출 화면 — #115

- 좌패널과 우패널의 기본 및 보조 탭 레이아웃을 각각 조사했습니다.
- `군사` 탭에서 긴 인물명이 `전법` 열을 침범하던 문제를 수정했습니다.
- `특성` 탭에서 긴 인물명이 `특성 1` 열을 침범하던 문제를 수정했습니다.
- 각 패널의 800px 한도 안에서 이름 열을 300px 또는 272px로 확장했습니다.
- 다른 열을 줄여야만 272px에 도달하는 탭은 기존 배치를 유지했습니다.

## 전법 설명 조합 공백 — #113

- `적 부대의병력 감소`, `자기 부대의방어 상승`처럼 조각 경계의 공백이
  사라지던 문제를 수정했습니다.
- 완성 문장마다 공백을 덧붙이지 않고 두 문자열을 조합하는 공통 서식 한 곳을
  `%s%s`에서 `%s %s`로 교정했습니다.

## 가와고에성 주변 지명 — #116

- 원문 `高麗(こま)`가 역사 국가명인 `고려`로 표시되던 두 연동 테이블을
  지명 독음 `고마`로 교정했습니다.
- 같은 목록의 `MSG_PK/JP/msgdata.bin` ID 10220과 `MSG/JP/strdata.bin`
  block 0 slot 10136만 수정하고 다른 `고려` 표기는 유지했습니다.

## 전체승인·고해상도 개전 — #117

- 등용 화면의 고정 이미지 `全承認`을 저·고해상도 각각 6개 상태 모두 공백 없는 `전체승인`으로 교체했습니다.
- 공식 원본 버튼 레이어를 보존하고 `SeoulHangang ExtraBold` 긴 표기 전용 규격(저 26px·고 52px, 장평 80%)으로 다시 렌더링했습니다. 체크와 `전`의 간격은 순정 `全承認`의 실제 코어 간격에 맞췄습니다.
- 고해상도 전투 시작 버튼의 `開戦`을 7개 상태 모두 `개전`으로 교체했습니다.
- 두 아틀라스는 대상 셀과 겹치는 BC7·BC3 압축 블록만 다시 만들고 나머지 리소스 바이트를 유지했습니다.

## 네비게이션 메뉴 휠

- 기본판·PK의 저해상도·고해상도 4경로, 150그룹·900개 배치를 전부 재구축했습니다.
- 기존 한국어 휠처럼 완성 이미지를 통째로 축소하지 않고, 공식 일본어 원본의 몸체·아이콘·상태 효과는 원래 크기로 보존했습니다.
- 일본어 글자만 분리한 뒤 선택한 B안 `SeoulHangang ExtraBold`로 한글 레이어를 렌더링했습니다. 폰트 장평은 바꾸지 않았습니다.
- `정보·임명·군사·내정·외교` 옆 방향표시 16그룹은 비문자 요소로 분리해 원본 픽셀을 보존했습니다.
- Imagen·ImageGen 등 생성형 이미지는 사용하지 않았습니다. 빈 글자 자리는 공식 JP/SC/TC/EN 아틀라스의 실제 공여 픽셀만 사용해 복원했습니다.
- 대상 논리 셀과 실제로 달라지는 BC3 블록만 다시 인코딩하고, 선택하지 않은 블록·텍스처·중첩 및 바깥 LINK 엔트리는 바이트 단위로 유지했습니다.

## 일반 버튼

- 저·고해상도 공용 일반 버튼 20종의 여섯 상태, 합계 240개 배치를 원본 레이어 분리 방식으로 재구축했습니다.
- B안 Medium을 적용해 저해상도 30px·고해상도 60px로 통일했으며, 모든 글자는 장평이나 버튼 크기를 줄이지 않고 1.0배로 들어갑니다.
- 승인·중지·닫기·부인·결정·거절·뒤로·재교섭·수락·다음의 아이콘과 버튼 몸체·상태 효과는 공식 원본 픽셀을 유지했습니다.
- `全解放`은 요청한 용어인 `전부해방`으로 번역했습니다.
- 생성형 이미지는 사용하지 않았으며, 일반 버튼 texture 1과 별개로 저·고해상도 `전체승인` BC7 아틀라스의 지정 셀만 갱신했습니다. `개전` texture 2는 보존했습니다.

## 설치 시 G1N 글꼴 선택

- 설치 설정에서 G1N `table 1` 글꼴을 `A: SeoulHangang ExtraBold (기존)` 또는 `B: 그리운 경찰감성체` 중 하나로 선택할 수 있습니다.
- 패처 화면에는 각 후보의 실제 96px G1N 글리프로 만든 무손실 PNG 미리보기를 ANSI 반블록으로 표시합니다. 색상 출력을 지원하지 않는 터미널에서는 단색 반블록으로 자동 전환합니다. JPEG 미리보기는 사용하지 않습니다.
- B안은 4개 리소스 아카이브의 0부터 세는 `table 1`만 교체합니다. `table 0`과 `table 2`, 문자 매핑·폭·베어링·포인터는 유지하며, 글꼴에 없는 `큿`은 기존 글리프를 유지합니다.
- 두 후보는 각각 독립된 공식 서명 리소스 번들이며 동시에 겹쳐 적용되지 않습니다. 이미 설치한 글꼴을 바꾸려면 메뉴 `2`로 원본을 복구한 뒤 다시 적용하십시오.

## 가상 히메의 긴 상속 성 이름 입력

- 기존 #62는 가상 히메 작명 화면의 한글 문자 검사를 허용했지만, 일본어판의
  성명 합계 6자 제한은 그대로 남아 있었습니다.
- `고바야카와`처럼 번역된 성이 5글자가 되면 공백을 포함한 상속 성이 제한을
  모두 차지해 이름 입력 가능 길이가 0이 되는 구조를 확인했습니다.
- 작명 화면 생성, 결정 시 합계 검증, 입력 안내의 세 한도를 모두 14자로
  맞췄습니다. 14자는 같은 실행 파일의 다른 이름 입력 경로와 UTF-16 14자 및
  종결자를 담는 30바이트 버퍼 계약에 맞습니다.
- 기존 한글 문자 허용 보정 네 곳은 바꾸지 않았습니다. PC 가로·세로
  일반/XInput 여섯 대상과 코드 배치가 다른 Steam Deck 실행 파일은 각각의
  정확한 선행 바이트를 확인한 뒤 보정합니다.
- 기존 v0.93.0 관리 설치본도 원본 복구 없이 v0.94.0 최종 실행 파일로
  안전하게 승격할 수 있으며, 메뉴 `2`의 검증 백업 복구 계약은 유지됩니다.
- 제보만으로 입력창을 여는 순간과 결정 버튼을 누르는 순간 중 정확한 충돌
  시점은 확정하지 않았습니다. 정적 용량 결함과 설치 경로는 보정했으며,
  실제 제보 저장에서 입력·결정·재로드를 확인하는 항목은 런타임 QA로 남깁니다.

## 패처

- 패처 제품 버전을 0.2.8로 갱신했습니다.
- v0.94.0 리소스 번들에 다음 열한 파일의 변경을 포함합니다.
  - `MSG_PK/JP/msggame.bin`
  - `MSG_PK/JP/msgui.bin`
  - `MSG_PK/JP/msgdata.bin`
  - `MSG/JP/strdata.bin`
  - `PARAM_PK/Person_Tenpou_Call_LeftInfo.bin`
  - `PARAM_PK/Person_Tenpou_Call_RightInfo.bin`
  - `RES_JP/res_lang.bin`
  - `RES_JP_PK/res_lang_exp_pk.bin`
  - `RES_JP_PK/res_lang_pk.bin`
  - `RES_JP_PK_PORT/res_lang_pk_port1.bin`
  - `RES_JP_PK_PORT/res_lang_pk_port2.bin`
- 가로형·세로형 일반/XInput 및 Steam Deck의 v0.93.0 정적 결과에 가상 히메
  성명 14자 보정을 누적합니다. 기존 v0.93.0 최종 해시에서 직접 승격하는
  경로와 순정 복구를 함께 검증했습니다.

## 이슈 연결과 작업 제외

- 이번 릴리스에서 해결하는 공개 이슈는 #113, #115, #116, #117, #118입니다.
- #119는 v0.93.0에서 이미 교정된 기본판 `MSG/JP/msggame.bin`을 v0.94.0이
  그대로 계승합니다. 해당 파일의 전후 SHA-256이 같은지 확인했으며 추가
  문자열 수정은 하지 않았습니다.
- 가상 히메 성명 용량 보정은 종료된 #62의 후속 조치입니다.
- 일본어 아틀라스 잔여 조사에서 도움말의 게임 화면 캡처 묶음은 전량 영구
  제외했습니다. 이 묶음은 v0.94.0 및 후속 이미지 재작업 파이프라인의 대상이
  아닙니다.
- 역사 타이틀 카드 `독안룡`은 레이어 분리 파이프라인의 실게임 시제품으로만
  기록하며, v0.94.0 공식 리소스 번들에는 포함하지 않습니다.

## 검증 기준

- 변경된 모든 탭 프로필의 최종 열 합계가 800px 이하인지 검사합니다.
- 원본과 결과 PARAM의 크기·레코드 수·SHA-256을 검증합니다.
- 외교 제안 보정은 지정된 메시지 리터럴 한 곳만 바뀌고 VM 제어 바이트와 다른
  모든 레코드가 유지되는지 검사합니다.
- 전법 설명 보정은 조합 서식 ID 2690 한 곳만 바뀌며 대표 조합 두 문장의 공백을
  검사합니다.
- 가와고에 지명 보정은 서로 대응하는 두 좌표만 `고마`로 바뀌고, 1,375개 연동
  지명 목록과 대상 밖 문자열이 그대로인지 검사합니다.
- `전체승인` 저·고해상도 12개 BC7 셀과 고해상도 개전 7개 BC3 상태가 지정 아틀라스에 반영되고,
  대상 밖 outer·texture·압축 블록이 그대로인지 검사합니다.
- 네비게이션 휠 900개는 일본어 글자 핵심 잔류 0, 전체 기하 이탈 0, 방향표시 16그룹의 원본 픽셀 차이 0을 검사합니다.
- 공용 일반 버튼 240개는 Medium 크기 축소 0, 아이콘 원본 픽셀 차이 0, 글자 영역 밖 변경 0, 셀 중첩 0과 `전부해방` 표기를 검사합니다. `전체승인`은 사용자 지정 예외인 26/52px·장평 80%, 순정 체크 간격 오차 0.75px 이하, 보호 마스크 겹침 0과 내부 글자 레인 이탈 0을 별도로 검사합니다.
- 정적 산출물과 BC3 통합 산출물을 각각 독립적으로 두 번 만들고 전체 산출물과 보고서 SHA-256이 일치하는지 검사합니다.
- Rust 워크스페이스 전체 테스트, `cargo fmt --check`, 경고를 오류로 처리한
  `cargo clippy --workspace --all-targets`를 통과했습니다.
- PC 여섯 정적 fixture의 원본 생성·v0.93.0 직접 승격·재적용·잘못된 선행
  바이트 거부를 검사했습니다. 서명된 PC 가로형 XInput과 Steam Deck 번들은
  격리 설치본에서 적용·재검증·순정 복구를 통과했습니다.
- 게임 프로세스를 종료한 상태에서 실제 Steam 설치본의 가로형 XInput과
  Steam Deck 실행 파일을 v0.94.0으로 승격하고, 리소스·정적 번들을 다시
  검증했습니다.

## Steam 적용 결과

2026-08-20에 게임과 런처가 종료된 상태에서 검증된 v0.94.0 정식 배포본을 Steam
설치본에 적용했습니다. 해상도는 변경하지 않았고, 이번 적용 뒤 게임을 실행하지
않았으므로 이미지 런타임 판정 결과를 새로 기록하지 않습니다.

- `NOBU16PK.exe`:
  `E53118961F95806B69A3122E091CDABF64D3904B991D427AE938C67CFF9AB4AF`
- `NOBU16PK_XINPUT.exe`:
  `E78D8C19D51AD3955519E8205032B98E42CA38F27C15713E3549E71641FC9A62`
- `NOBU16PK_SD.exe`:
  `9F90798F23FD3B1FDCEA4D3E075E953A78F42E49003AE218CC45CE0EFD347A8B`
- `N16KoreanPatcher.exe` 0.2.8:
  `BD3C634E428C1AE18304911C030A45FB23B1C722E0BB99EEF42D7CD7085179A7`

설치 상태에는 리소스, 가로형 XInput, Steam Deck 세 릴리스가 모두
`release_version: 0.94.0`으로 기록됐습니다. 실제 제보 저장의 가상 히메
입력·결정·재로드 확인은 해당 저장을 확보하면 계속 수행할 후속 런타임
점검입니다. 정적 용량 결함과 설치 경로 보정은 이번 릴리스에 포함됩니다.

## 배포 파일

- 파일: `NOBU16_PK_Korean_Patch_Steam_1.1.7_v0.94.0.zip`
- 크기: 929,815,750바이트
- SHA-256: `EC48E7C9F49B7D99C40744A45E055CBB1ACC11F46B115BC685AAAE64F57C9E8B`
- `N16KoreanPatcher.exe` 0.2.8 SHA-256:
  `BD3C634E428C1AE18304911C030A45FB23B1C722E0BB99EEF42D7CD7085179A7`
