# v0.93.0 릴리스 노트

v0.93.0은 Steam 일본어판 PK 1.1.7용 한국어 패치입니다. 이번 릴리스는 Steam Deck용 실행 파일을 정식 설치 흐름에 포함하고, 전투 부대명 중복 표현과 가로형 지도 아이콘 배치를 교정했습니다. v0.92.1까지 제공한 번역·독음·화면 표시·XInput 개선도 모두 포함합니다.

## 주요 변경 사항

- `NOBU16PK_SD.exe` 자동 패치를 추가했습니다. PC 구성 선택과 관계없이 Steam Deck 실행 파일에 필요한 공통 한국어 표시 보정이 함께 적용됩니다.
- Steam Deck 실행 파일에는 이름 입력, 상단 정보 표시, 이벤트 대사와 화자 이름표, 이름칸 너비, 지행 간격 보정만 적용합니다. 가로 성이름 관련 지도 패치와 PC 전용 XInput 패치는 제외해 기본 패드 입력 경로를 유지합니다.
- Steam Deck 게임 폴더에서 Proton/Wine으로 패처를 직접 실행할 수 있습니다. Windows 전용 설치 기록 확인은 생략하지만, 공식 번들 서명과 지원 원본 파일 검증은 유지합니다.
- 전투 메시지에서 완성된 부대명 뒤에 `부대` 또는 `군`이 중복되던 기본판 323곳과 PK 273곳, 합계 596곳을 교정했습니다.
- 가로형 지도에서 성하 시설 건설 망치 아이콘이 주변 아이콘 수에 따라 세로형 위치로 돌아가던 문제를 모든 활성 배치에서 수정했습니다.
- 지행 지정 화면 간격, 출진 부대 지도 명패, 긴 성주명 자동 맞춤, 가문 정책 독음, 혼인 보좌 대사와 조사 표현 등 v0.92.1까지의 수정 사항을 계승합니다.

## 설치와 업그레이드

1. 게임과 런처를 완전히 종료합니다.
2. 기존 관리 설치본이 있으면 이전 패처의 메뉴 `2`로 원본을 복구합니다.
3. v0.93.0 ZIP 전체를 `NOBU16PK.exe`가 있는 게임 폴더에 풉니다.
4. `N16KoreanPatcher.exe`를 실행해 메뉴 `1`에서 원하는 구성을 적용합니다.

Steam Deck에서는 데스크톱 모드에서 같은 ZIP을 `steamapps/common/NOBU16`에 풀고 Proton/Wine으로 패처를 실행할 수 있습니다. Steam Deck용 실행 파일은 별도 선택 없이 자동 패치되며, 설치 후에는 평소처럼 Steam에서 게임을 실행하면 됩니다.

## 검증

- 최종 패처가 더블클릭 실행 시 통합 메뉴를 표시하고 정상 종료되는 것을 확인했습니다.
- 서명 번들 검사, 격리 적용·상태 확인·복구, Rust 테스트와 정적 분석을 통과했습니다.
- Windows에서 Steam Deck와 같은 실행 환경으로 패치된 SD 실행 파일을 완전히 재실행해 640×360 타이틀 화면과 한국어 이벤트 대사 진입을 확인했습니다. 해상도 변경은 없었습니다.
- 성하 시설 건설 아이콘은 3840×2160에서 해상도 변경 후 게임을 완전히 종료하고 재실행한 상태로 서로 다른 두 배치를 확인했습니다.
- 최종 ZIP은 새 폴더에 다시 풀어 모든 파일을 `SHA256SUMS.txt`와 대조했습니다.
- 실제 Steam 설치본의 리소스, Steam Deck 실행 파일과 가로형 XInput 실행 파일이 모두 최종 목표 상태로 확인됐습니다.

## 호환성 주의

- Steam 일본어판 PK 1.1.7, Steam build 18823764만 지원합니다.
- ZIP의 일부 파일만 복사하지 말고 전체를 같은 게임 폴더에 풀어야 합니다.
- 해상도를 바꾼 뒤 화면 표시를 확인할 때는 게임을 완전히 종료하고 다시 실행하십시오.
- 지원하지 않는 원본이나 변조된 파일이 발견되면 패처는 적용을 시작하지 않습니다.

## 배포 파일

- `NOBU16_PK_Korean_Patch_Steam_1.1.7_v0.93.0.zip`
- 크기: 539,638,140바이트
- SHA-256: `9E1CD3BCE0CB8044170DD536F0EA490739A5BFEC7C55CCF6A63C8D969E93300D`
