# v0.90.4 릴리스 노트

v0.90.4는 v0.90.3의 한글 리소스와 일반 PC 실행 파일은 유지하면서,
`NOBU16PK_XINPUT.exe`의 컨트롤러 초기화와 A/B 입력·안내 표시를 바로잡은
컨트롤러 안정화 릴리스입니다.

## 변경 사항

- XINPUT 실행 파일의 진입 시점에 컨트롤러 모드를 초기화해 게임이 마우스
  입력만 받는 상태로 시작되던 문제 수정
- 가로형과 세로형 XINPUT 실행 파일 모두에서 방향 입력 활성화
- 물리 `A`를 승인, 물리 `B`를 뒤로가기로 통일
- 게임 화면의 A/B 조작 안내와 실제 입력 동작을 일치시킴
- XINPUT판의 마우스 커서는 숨긴 채 컨트롤러 입력 경로 유지
- 일반 `NOBU16PK.exe`의 마우스 입력 경로는 변경하지 않음
- v0.90.3에만 적용했던 Steam 관리 설치 경로 검사 임시 우회를 제거하고
  정식 Steam AppManifest·설치 경로 검사를 복구
- 순정 파일 SHA-256, 패치 번들 서명, 결과 파일 해시 검증 유지

## 컨트롤러 실행 방법

1. 패처에서 `컨트롤러 지원 → 실험적 지원 사용`을 선택합니다.
2. 생성된 `NOBU16PK_XINPUT.exe`를 Steam 라이브러리에 비 Steam 게임으로
   등록합니다.
3. 등록한 항목의 컨트롤러 설정에서 Steam Input을 켭니다.
4. Steam Input 레이아웃은 기본 **게임패드** 템플릿을 사용합니다.
5. 컨트롤러를 먼저 연결한 뒤 Steam에서 `NOBU16PK_XINPUT.exe`를 실행합니다.

이전 시험 중 A/B를 수동 교환한 Steam Input 레이아웃을 사용했다면 물리 버튼이
다시 뒤집힐 수 있습니다. 이 경우 레이아웃을 기본 게임패드 템플릿으로
복구하십시오.

XINPUT판에서는 마우스를 사용할 수 없습니다. 마우스로 플레이할 때는 일반
`NOBU16PK.exe`를 실행하십시오.

## 설치 전 주의

- Steam JP PK v1.1.7, Steam build `18823764` 전용입니다.
- v0.90.4는 순정 Steam JP PK v1.1.7에서 새로 적용하는 클린 설치만
  지원합니다.
- 이전 패치가 적용되어 있다면 먼저 해당 패처로 원본 복구하거나 Steam 파일
  무결성 확인을 완료한 뒤 v0.90.4를 적용하십시오.
- 세로형·가로형 또는 컨트롤러 지원 여부를 바꿀 때도 먼저 패처 메뉴 `2`로
  원본 복구한 뒤 다시 적용하십시오.
- 번들 파일명은 서명된 v0.90.2 번들을 재사용하지만, 패처가 검증된 v0.90.4
  XINPUT 후처리를 적용하고 설치 상태를 `0.90.4`로 기록합니다.

## 검증 결과

- 배포 ZIP 크기: `522,274,884` bytes
- 배포 ZIP SHA-256:
  `4564B2AB72E3F6AB9AF9F94BB4E12C9F3D5460AD13B2EDAEE3CF25D10C022212`
- 가로형 `NOBU16PK_XINPUT.exe` SHA-256:
  `75B4C9BB234EA24A869D2E3D7F1BBE04718571D22F6A4CF5612CD5CE322EABC7`
- 세로형 `NOBU16PK_XINPUT.exe` SHA-256:
  `2C42127A04318E5F51891FB2072117E8F452A52935D4D2EEBD481FA3185767E4`
- 배포 패처 `N16KoreanPatcher.exe` SHA-256:
  `FF48889F38019A53FE2BFEE115B1489FA496E8D4D57BDAECCB60274665557654`
- 클린 생성물 계약, 컨트롤러 초기화 위치, A/B 교환 위치와 조작 안내 위치
  자동 검증 통과
- 실제 Steam 실행에서 방향 입력, A 승인, B 뒤로가기 및 화면 안내 일치 확인
